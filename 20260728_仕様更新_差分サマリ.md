# 2026-07-28 仕様更新 差分サマリ

タイムレコード CSV取込機能。エンジニアからの確認事項3件への回答に加え、テンプレート列定義をアプリ側実装と突き合わせて検出した課題を全件確定させた。

**照合基準ブランチ: `rakmy/rakmytimerecord` の `origin/feature/189`**（最終コミット 2026-07-10）
`main` / `develop` には未マージ。`main` と照合すると §7.3 が誤りに見えるため、確認時は必ずブランチを合わせること。

---

## 1. 最初の3件の確認事項への回答

| # | 質問 | 回答 |
|---|---|---|
| 1 | ①〜③設定マスタに「すべて」「エラー」タブは不要か | **不要。** ①〜③のタブは種別切替（業態/店舗/雇用区分）、④⑤のタブは行ステータス。軸が違うため混在させない。①〜③のエラーは赤バナー＋行内3択アクションで表示（既存パターン）。なお④⑤のタブは**実装では実際にフィルタさせること**（モックが表示のみなのはモックの制約） |
| 2 | 給与形態に日給を追加するか | **追加しない。モック側が誤り。** DBは `pgEnum("employment_category", ["時給","月給"])` の2値。`profile/route.ts` が else 分岐のため日給を足すとサイレントに月給扱いになる。経営管理側も2区分で「日給月給制」は月給の一種。→ モックを2値に修正済み |
| 3 | DB取り込み失敗時は汎用メッセージ＋リトライでよいか | **不可。行単位の成否開示が必須。** `neon-http` は `db.transaction()` 非対応でバッチを巻き戻せないため、汎用メッセージだと成功分がDBに残ったまま全件やり直しを促すことになる。部分成功画面＋失敗行テーブル＋失敗行CSVで回収させる |

---

## 2. 確定した仕様（README §7.7 / §8 / §9）

### 最重要3件

**§7.7 ロールバック粒度＝行単位の補償削除**
バッチ全体は巻き戻さない。成功行はDBに残す。失敗行はその行が書き込んだレコードを**すべて**補償削除する（Clerk招待の revoke を含む）。

④従業員は1行が3ステップに分かれるため、**「行単位」＝3ステップ全体**。

| ステップ | 処理 | 失敗時に補償削除する対象 |
|---|---|---|
| 1 | `POST /api/employees`（＋Clerk招待） | なし |
| 2 | `PUT /api/employees/[id]/commuting-allowance` | ステップ1のユーザー＋店舗割当＋Clerk招待 |
| 3 | `PUT /api/employees/[id]/stores`（複数店舗時） | ステップ1・2のすべて |

補償削除自体が失敗した行は「要手動確認」として別枠表示し、再取り込み対象から除外する。

**§8.1 従業員コード・雇用区分コードのUNIQUEが全社横断**
`users` / `employment_types` の `employee_code` が `company_id` との複合ではなくテーブル全体でユニーク。

- 突合は**必ず `companyId` で絞る**（§4.3 に明記）。全社横断で突合すると他社レコードを上書きする
- 配布テンプレートのサンプル行のコードを**空欄化済み**（従来は `EMP-001` 等が入っており、2社目以降が必ず衝突した）
- 自動採番規則: `EMP-{companyId先頭8文字}-{連番4桁}` / `ETP-{companyId先頭8文字}-{連番4桁}`

**§8.2 従業員1件の取込に最大3リクエスト**
`createEmployeeSchema` に交通費フィールドがなく、専用エンドポイントが別にある。§7.5 の上限根拠（1行につきClerk招待1回）が実態と乖離していた。
→ **④従業員のみ上限を200行に下げる**（他は500行のまま）。

### その他の確定事項

| § | 項目 | 決定 |
|---|---|---|
| 8.3 | 複数店舗勤務 | ④は単一店舗のまま。複数は取込後に画面から追加。④⑤の非対称は意図的（⑤の担当店舗＝権限範囲、④の店舗名＝主たる勤務先） |
| 8.4 | 月みなし残業時間 | `employment_types.shift_restriction`（text型）にマッピング。0〜999の整数バリデーションを取り込み側で担保 |
| 8.5 | 業態名・店舗名の突合 | 会社内・名称の完全一致（前後空白のみトリム）。DB側に同名が複数ある行はエラー |
| 8.6 | 雇用区分キーの or | コードがあればコード優先、空欄なら名前。名前で複数一致はエラー |
| 8.7 | CSVの空欄セル | **「変更しない」。NULL上書きはしない。** 必須★列が空欄の行はエラー |
| 8.8 | 列バリデーション | 実効上限はAPIスキーマ側に合わせる（従業員名は255ではなく**100**）。金額は全角・カンマ・円記号を正規化。電話番号は必須★に寄せる |
| 8.9 | 退職者との突合 | `retired` / `inactive` と同コードの行は**エラー**。自動復活させない。`inviting` はエラーではなく差し替え |
| 8.10 | 給与体系の日給 | **削除**。月給/時給の2値に統一 |

---

## 3. モックに追加した状態遷移（README §9・6件すべて実装済み）

`index.html` は正常系と行レベルのエラーは網羅していたが、以下が無かった。

| § | 状態 | 実装 | デモ操作 |
|---|---|---|---|
| 9.1 | 取り込み実行中 | `#import-running-overlay` / `runImport()` | インポート実行ボタンを押す |
| 9.2 | 失敗行CSVダウンロード | `downloadFailedRowsCsv()` / 完了画面4箇所 | 完了画面のデモトグル「一部失敗(429)」 |
| 9.3 | 上限超過の受付拒否 | `demoUploadReject()`（全画面へ動的注入） | 各アップロード画面のデモ「行数超過」「サイズ超過」 |
| 9.4 | 全件エラー（0件） | `demoZeroTarget()`（全画面へ動的注入） | 各プレビュー画面のデモ「全件エラー(0件)」 |
| 9.5 | ファイル形式エラー | `UPLOAD_REJECT_MSG` | 各アップロード画面のデモ「文字コード」「列不一致」「0件」 |
| 9.6 | 権限による出し分け | `setHubRole()` | インポートハブのデモ「オーナー」「管理者(店長)」 |
| 9.7 | 除外行の完了画面での開示 | `renderExcludedRowsBlocks()` / 完了画面4箇所 | 完了画面を開くと常時表示 |

**エラーは発生タイミングで3種類あり、出る画面が違います。** ①アップロード時（行数超過など）＝受付拒否バナー、完了画面には出ない／②プレビュー時（値不正・退職者・担当店舗外）＝完了画面の「取り込まなかった行」で開示（§9.7で追加）／③実行時（429・キー衝突）＝失敗行テーブル＋CSV。**失敗行CSVには②③の両方が含まれます。**

**代表画面ではなく全画面に横展開しています。** 受付拒否バナーと0件状態はアクティブ画面へ動的注入する共通実装で、静的画面・ウィザード・動的生成される単体取込画面のいずれでも同じ挙動になります。デモ操作列も対象画面すべてに自動で配られます。

### 特に重要な2件

**9.1 実行中の状態** — 進捗もスピナーも無く、**実行ボタンの二重押下防止も無かった**（既存の `disabled` は「必須項目未達で次へ進めない」用途のみ）。連打で多重実行される状態だった。
オーバーレイ＋プログレスバー＋`beforeunload` 警告＋`_importRunning` フラグを追加。

> 実装時はモックの擬似進捗ではなく、**サーバーから返る実際の処理済み件数**で更新すること。リクエスト単位ではなく行単位でカウントする。

**9.2 失敗行CSVダウンロード** — 「失敗した1件を再取り込み →」がアップロード画面の先頭に戻すだけで、**失敗行だけのCSVを作る手段が無かった**。デモは1件なので成立して見えるが、50行失敗したらユーザーは画面の表を見ながら元CSVから手で拾うことになる。
§7.7 の「再取り込みで回収できます」は、この動線なしでは成立しない。

---

## 4. アプリ側（`rakmy/rakmytimerecord`）への依頼

**いずれもインポート機能のリリースを待たない。** 現行API構成のままで実装できる仕様にしてある。

| 優先度 | 内容 | 効果 |
|---|---|---|
| 中 | `createEmployeeSchema` を交通費まで受け取れるよう拡張（§8.2） | ④のリクエスト数が1/2〜1/3に。§7.7 の補償削除の分岐がほぼ消え、上限を500行に戻せる |
| 中 | `business_types.label` / `stores.name` に `(company_id, name)` の複合ユニーク追加（§8.5） | 同名重複を入口で防げる。現状はインポート側で後始末している |
| 中 | `(company_id, employee_code)` の複合ユニークへ移行（§8.1） | 採番規則から会社識別子プレフィックスを外せる |
| 低 | `shift_restriction` → `deemed_overtime_hours`（integer）へリネーム＋型変更（§8.4） | カラム名から用途が読めず、将来の担当者が必ず誤解する |
| — | `feature/189` を `main` へマージ | `stores.business_hours` が `main` に無く、テンプレート②の営業時間列が成立しない |

---

## 5. 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `README.md` | §1.2 列定義（給与体系2値・コード空欄推奨・店舗単一）／§4.3（companyIdスコープ・空欄セルの意味）／§7.5（④のみ200行）／§7.7（多段呼び出し前提で全面改訂・失敗行CSV追加）／**§8 新設（10件）**／**§9 新設（6件）** |
| `index.html` | 日給の削除（プレビュー7箇所・options配列・許可値表示・テンプレDL生成）／サンプルコードの空欄化／**§9の6状態を新規実装（全画面横展開）**／取込上限の種別別出し分け `rowLimitOf()`／完了画面の「次にやること」4箇所／「重複時の処理」への空欄セル注記3箇所／退職者・担当店舗外のエラー行／`demoResult()` コメントの不整合修正 |
| `③雇用区分.tsv` | `日給スタッフC / 日給 / 10000 / 8` → `アルバイトC / 時給 / 1100 / 空欄`、コード空欄化 |
| `④従業員.tsv` | 雇用区分名を `アルバイトC` に、コード空欄化 |
| `入力ガイドv2.tsv` | 給与体系の許可値を `月給 / 時給` に |
| `設定データインポートマッピング表.tsv` | 同上 |
| `CHANGELOG.md` | 上記の変更履歴 |

> 経営管理側のマッピング先（`給与.日給月給制` / `給与.日給制`）は**変更していない**。これは経営管理のフィールド名であり、タイムレコードの給与体系とは別物。
