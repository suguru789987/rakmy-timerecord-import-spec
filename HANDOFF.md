# 引き継ぎノート（タイムレコード import-spec）

> **2026-07-31 更新。PdM（@suguru789987）の最終出社は 2026-08-19。以降、本仕様を維持・更新する担当は不在となる。**
>
> このノートは「**担当がいない状態で実装を進めるための取扱説明書**」です。実装で迷ったら、まずここに戻ってください。

---

## 0. 最初に知っておくこと

### 実装対象はテンプレートインポートのみ

| 対象 | 状態 |
|---|---|
| **テンプレートインポート（README §1）** | **実装対象。仕様は確定済み** |
| 外部勤怠サービスインポート（README §2） | **⛔ 凍結。仕様未完成のため着手しないこと** |

外部勤怠は仕様が初版（2026-03-31）からほぼ更新されておらず、未定義の論点が6件残っています（README §10）。**半端に存在する仕様を実装根拠にしないでください。** 再開する場合は §10 の解消から始めてください。

モックの `s-saas*` 系19画面と補完画面 `s-tpl-complete` には凍結バナーが出ます。それらは実装対象ではありません。

### 迷ったときの優先順位

1. **README §0 前提条件** — この仕様が立脚する前提7項目。**一般的なSaaSの常識で判断しないこと**
2. **DBスキーマ（`rakmy/rakmytimerecord`）** — enum・ユニーク制約・NOT NULL・型と桁。これだけがどう実装しても回避できない制約
3. **モックの `s-tpl*` 系画面** — UI挙動の正
4. **テンプレートTSV** — CSV列・型・必須の正

矛盾したときは上が優先です。

### 照合基準ブランチ

**`rakmy/rakmytimerecord` の `origin/feature/189`**（最終コミット 2026-07-10）。`main` / `develop` には**未マージ**です。`main` と照合すると README §7.3 が誤りに見えます。

---

## 1. 未決事項（担当不在のため、エンジニア判断で確定してよい）

### 1.1 【最優先】インポートは手入力の登録処理を再利用するか

README §11.1 に詳細。**この判断で仕様の6箇所が変わります。**

| 観点 | 再利用する | DBへ直接書く |
|---|---|---|
| 業務ロジック（招待メール・Clerk連携） | そのまま使える | 作り直すか書き漏らす |
| ④の1行あたりの処理回数 | 最大3回 | 1回 |
| ④の上限行数 | 200行 | 500行でよい |
| 失敗時の巻き戻し | 3ステップ全体が1単位 | 単純 |
| 列の桁上限 | zod に従う（名前100文字） | DBに従う（255文字） |
| 担当店舗 | 置換されるためマージ要 | 消さないように書けばよい |

現在の仕様は「再利用する」前提で書き、各所に条件を明記してあります。

**PdM不在後は、エンジニアの判断で確定して構いません。** 判断したら README §7.1 §7.5 §7.7 §8.2 §8.8 §4.4 の該当箇所を実装に合わせて更新してください。

### 1.2 暫定値（実運用で見直してよい）

根拠が薄いものには仕様書に「暫定値」「設計判断」と明記してあります。**実装後の計測や運用実態にもとづいて変更して構いません。**

| 項目 | 現在値 | 備考 |
|---|---|---|
| ④従業員の上限行数 | 200行 | 1.1 の判断次第。性能実測なし |
| 月みなし残業時間の上限 | 999 | 数値であること以外の根拠なし |
| 金額の正規化ルール | 全角・カンマ・円記号を除去 | 既存の正規化処理は未確認 |
| 業態名・店舗名の突合 | 全角半角を正規化しない | 既存の突合ロジックは未確認 |

### 1.3 未確認事項

- **雇用区分コードに自動採番があるか**（従業員コードには既存実装あり。README §8.1）
- **`PUT /api/employees/[id]/stores` が置換か追加か**（置換なら README §7.1 と同じ考慮が必要）

いずれも実装前にコードを確認してください。1.1 で「DBへ直接書く」を選ぶ場合は不要になります。

---

## 2. アプリ側（`rakmy/rakmytimerecord`）への依頼

README §11.2 に詳細。**1件目はブロッカー、2件目は不具合です。**

| 優先度 | 内容 |
|---|---|
| **ブロッカー** | `feature/189` を `main` へマージ。`stores.business_hours` が `main` に無く、テンプレート②の営業時間列が成立しない |
| **高・不具合** | 自動採番の衝突チェックが会社内に絞られている（`src/app/api/employees/route.ts:293,300`）。DB制約は全社ユニークのため、他社のコードと衝突するとすり抜けてDB制約違反になる |
| 中 | `createEmployeeSchema` を交通費まで受け取れるよう拡張（1.1 で「再利用する」を選んだ場合に効く） |
| 中 | `business_types.label` / `stores.name` に `(company_id, name)` の複合ユニークを追加 |
| 中 | `(company_id, employee_code)` の複合ユニークへ移行 |
| 低 | `shift_restriction` → `deemed_overtime_hours`（integer）へリネーム＋型変更 |

---

## 3. 仕様を変更するときの手順

### 必ず整合チェックを実行する

```
python3 audit_spec_mock.py
```

### 実装・検証の達成度を見る

```
python3 progress.py
```

受け入れ条件表の `実装状況` と `判定` を集計し、**マイルストーンM1〜M6の達成度とリリース可否**を出します。MVP必須32件がすべて合格するまでリリース判断はできません。

終了コード0で整合、1で不整合箇所が一覧表示されます。**仕様書かモックを変更したら必ず実行してください。**

検査内容：

- README が挙げる関数名・画面ID・要素IDがモックに実在するか
- 配布先の画面数（完了画面4／プレビュー4など）
- 適用範囲ラベルの網羅と語彙の固定（4語彙以外を使うと落ちる）
- **方式の交差汚染**（テンプレート側の仕様が外部勤怠の画面を実装例に引用していないか）
- §0 前提条件の存在と7項目の記載
- 仕様が「BOM付きUTF-8」と書いているなら実際にBOMが入っているか

### 過去に繰り返した失敗

このリポジトリで実際に起きた失敗です。**同じことを繰り返さないでください。**

| 失敗 | 何が起きたか |
|---|---|
| **実装を確認せず一般論で仕様を書いた** | 給与体系に「日給」を入れていた。外部サービスの語彙をそのまま許可値にしたもので、DBは2値。採番規則も既存実装があるのに独自に定義していた |
| **外部勤怠の画面をテンプレート側の実装例に引用した** | エラー行の「行内3択アクション」がそれ。先行実装に存在しない機能を要求し、エンジニアの工数見積もりが膨らんだ |
| **レコード単位でしか定義せず、列単位を書かなかった** | 「上書き更新」でどの列が更新されるか未定義だった。空欄セルの意味、担当店舗の置換も同じ形の欠落 |
| **仕様に書いてモックに実装しなかった** | BOM付きUTF-8、完了画面の「次にやること」など。数を数えずに「4箇所に実装」と書いたこともある |

**共通する原因は「確認できるものを確認せずに埋めた」ことです。** 迷ったら §0 前提条件とDBスキーマに戻ってください。

---

## 4. 5テンプレートの構成（2026-07-28 時点）

| テンプレート | 列数 | 列 |
|---|---|---|
| ① 業態 | 1列 | 業態名 |
| ② 店舗 | **5列** | 店舗名／所属業態／郵便番号／住所／営業時間 |
| ③ 雇用区分 | 6列 | 雇用区分名／雇用区分コード／給与体系／金額／月次契約労働時間／月みなし残業時間 |
| ④ 従業員 | 8列 | 従業員名／電話番号／メールアドレス／従業員コード／雇用区分名／店舗名／交通費単位／交通費 |
| ⑤ 管理者 | **3列** | 管理者名／メールアドレス／担当店舗 |

依存関係：②は①（所属業態）、④は②③（店舗名・雇用区分名）、⑤は②（担当店舗）を参照。取り込み順序は ①→②→③→④→⑤。

列単位の書き込み仕様（どの列がどのDBカラムへ書かれるか）は **README §4.4** にあります。

---

## 5. リポジトリの構成

| ファイル | 役割 |
|---|---|
| `README.md` | **仕様の正本**。§0 前提条件 → §11 まで。冒頭に日付別の変更点一覧 |
| `20260803_CSV一括登録機能_仕様書（PdM向け）.md` | **まずここから読む**。概要・操作フロー8本・仕様判断の理由・やらないと決めたこと・過去の失敗と教訓・検証の範囲 |
| `index.html` | 画面モック。`s-tpl*` 系が実装対象 |
| `audit_spec_mock.py` | 仕様書とモックの整合チェック |

> 2026-08-03: 旧版の `scripts_audit_spec_mock.py`（検査項目が少ない26行版）と旧モック `import-mock.html`（v2・凍結バナー無し）を削除しました。**モックは `index.html` だけです。**
| `CHANGELOG.md` | 変更履歴と決定理由 |
| `JUDGMENT_LOG.md` | PdM判断の記録 |
| `20260728_エンジニア質問_回答と対応.md` | エンジニアからの確認事項3件への回答と対応 |
| `20260804_受け入れ条件_定義表.tsv` | **何を守るか・なぜか**（74件）。設計ポイント／MVP区分／崩れると起きること。**変わらない側** |
| `20260804_受け入れ条件_確認表.tsv` | **実装が条件に合致しているかの確認**（74件）。実装箇所・合致条件・根拠の検証ID・実装状況・判定 |
| `20260804_受け入れ条件_使い方.tsv` | **定義表と確認表の使い方。** 記入手順・判定ルール・確認方法の3種・迷ったときの対処（61行） |
| `20260803_検証プラン_操作系.tsv` | 操作手順と期待値49件。10列（記入するのは実際の結果・判定の2列だけ） |
| `20260803_検証プラン_計算系.tsv` | 数値・件数・上限の確認19件。9列（記入するのは実際の値・判定の2列だけ） |
| `20260803_検証用データセット.tsv` | CSVに入力するデータ25件（D1〜D5）。検証プランからIDで参照 |
| `20260803_トレーサビリティ表.tsv` | 仕様の節と受け入れ条件・検証IDの対応34節 |
| `20260803_ヘルプページ_CSV一括登録_ドラフト.md` | ヘルプページの原稿。Framer掲載用。スクショマーカー11箇所 |
| `20260804_Notion掲載用_CSVで一括登録する.md` | **Notionに貼り付ける版**。作業用の節を除いた本文のみ。ドラフトから生成 |
| `ラクミータイムレコード_インポートテンプレート_*.tsv` | 配布テンプレート（列定義の正） |

**ライブモック（2つあります）**

| モック | URL | 扱う範囲 |
|---|---|---|
| **CSVインポート**（本リポジトリ） | https://suguru789987.github.io/rakmy-timerecord-import-spec/ | 本仕様の対象。`s-tpl*` 系が実装対象 |
| **登録・編集の全体**（owner-mock） | https://suguru789987.github.io/rakmy-owner-registration-mock/ | 手入力の登録・編集・削除、初期セットアップ、権限、打刻 |

主要ディープリンク
- テンプレート取込（初期一括）: `.../#tpl-setup`
- 運用インポートのハブ: `.../#s-top`

---

## 6. 全体の入口

タイムレコード全体の正本は **owner-mock** 側にあります。

https://github.com/suguru789987/rakmy-owner-registration-mock/blob/main/HANDOFF.md

本リポジトリはインポート機能の仕様のみを扱います。権限階層・登録フロー・打刻画面などはそちらを参照してください。

## 資料を直したら必ず実行する

```
python3 sync_docs.py    # TSV → 仕様書の件数・列数を書き直す
python3 sync_excel.py   # TSV → 配布用エクセル（01・02）を同期
python3 check_all.py    # 全資料の整合を検査
```

> **仕様書に件数や列数を手で書かないでください。** `sync_docs.py` が実データから生成します。
> 数字を書く場所を増やしたら `sync_docs.py` の `RULES` に1行足します。文言を変えて置換できなくなった場合は、黙って通らずエラーで止まります。

**件数や列数は複数の資料に複製されています。** 受け入れ条件を1件でも直すと、
仕様書・使い方.xlsx・エクセルの集計が同時に古くなります。目視では追えません。

検査するもの:

- 定義表 ↔ 確認表（条件ID・フロー・MVP区分・マイルストーン・満たすべきこと）
- 合致条件や満たすべきことが重複していないか
- 受け入れ条件の根拠が実在する検証IDか／参照されない検証項目がないか
- 検証プランとデータセットの相互参照
- 仕様書に書かれた件数・列数が実データと一致するか（15箇所）
- ヘルプの掲載本文に内部メモ・丸数字・旧表記が残っていないか、節番号が連番か
- デスクトップの配布物がリポジトリと一致するか

終了コード0で整合。1件でも見つかれば1を返します。
