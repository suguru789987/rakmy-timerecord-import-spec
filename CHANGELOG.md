# CHANGELOG — タイムレコード import-spec

タイムレコード CSV 取込フロー（テンプレート／外部勤怠サービス連携）モックと仕様の変更履歴。
判断経緯＝`JUDGMENT_LOG.md`。全体の入口・正本は owner-mock（`rakmy-owner-registration-mock`）。

---

## 2026-07-28

### Added（インポート実行時の失敗ハンドリングを確定 — README §7.7）
- **ロールバック粒度を「行単位の補償削除」で確定**：バッチ全体は巻き戻さず（`neon-http` が `db.transaction()` 非対応）、成功行はDBに残す。DBのinsertは成功したが後続のClerk招待APIが失敗した行は、**そのDBレコードを補償削除**して招待未送信の孤児レコードを残さない。
- 不採用案：DBレコードを残して招待未送信状態にする案。再取り込み時に「差し替え」判定となり、完了画面の確定文言「失敗した行はDBに残っていません」と矛盾するため。
- **エラーの切り分けを3分類で定義**：行単位で切り分け可能な失敗（部分成功として行ごとに失敗理由を表示）／プレビュー後のキー衝突（`既に登録済みのため取り込めませんでした（他の操作で登録された可能性があります）`）／行単位に切り分けられない障害（全件失敗・`データは1件も登録されていません` を明示）。
- **汎用メッセージ＋リトライ促しは不採用**と明記：バッチを巻き戻せない以上、成功分がDBに入ったまま全件やり直しを促すことになり二重登録の原因になる。

### Fixed（章レベルの混同を解消 — §3 補完は外部勤怠専用だった）
- 前回は §7〜§9 の決定事項にのみ適用範囲を付与したが、**§1〜§5 の章レベルでも方式の帰属が書かれていなかった**ため、§1／§3／§4.1／§4.2／§4.3／§5 に適用範囲を明記した。
- **§3「マッピング不可項目の補完」が外部勤怠専用の機能であることが未記載だった。** テンプレートインポートには「マッピング不可項目」が存在しない（テンプレートは必須列をすべて持つ）ため本章は適用されない。補完の実行に④従業員テンプレートの「上書き更新」を機構として使うため、テンプレート機能と誤読しうる状態だった。
- **モックの補完画面のIDが `s-tpl-complete` とテンプレート名前空間になっているが、遷移元は `s-saas4`（外部勤怠の完了画面）のみ**であることを確認。命名が実態と合っていないため §3・§10 に明記し、後続フェーズで `s-saas-complete` への改名を検討事項とした。**この画面もフェーズバナーの対象に追加**（対象19枚、先行実装はテンプレート側10画面）。
- §10 の検討事項に「§3 補完機能」を追加（補完テンプレートの列生成規則が未定義である点も含む）。
- `audit_spec_mock.py` に**適用範囲の網羅チェック**を追加。§7.1〜§9.7 の全小節と §1／§3／§5 に「適用範囲」の記載があるか、補完画面がフェーズバナーの対象かを機械的に検査する。

### Added（全決定事項に適用範囲を付与し、外部勤怠の未定義論点を §10 に分離）
- **テンプレートインポートと外部勤怠インポートの混同を解消。** 従来 §7.1〜§9.7 の**全24件に適用範囲の記載がなく**、本文の言及頻度から推測するしかない状態だった。§8.3 のようにテンプレート④の列仕様しか書いていない項目でも外部勤怠に適用されると読めてしまっていた。
- **全24件の冒頭に「適用範囲」を明記**：`両方式共通`（DB制約・API仕様・画面挙動に起因）／`テンプレートのみ`（列定義・ファイル配布に起因）／`外部勤怠のみ`／`方式別に定義済み`。
- **§10「後続フェーズ（外部勤怠サービスインポート）の検討事項」を新設。** 先行フェーズの決定が外部勤怠に直接適用できない、または未定義の項目を5件一覧化した。
  - **失敗行CSVの書式（§9.2）** — 「元テンプレートと同一列」はテンプレート前提。外部勤怠は列マッピングを経るため「元テンプレート」が存在せず、取込元CSVの列で出すのかマッピング後のラクミー列で出すのかが未定義だった。にもかかわらず実装は外部勤怠側の完了画面にも同じボタンを置いていた
  - **コード衝突の予防（§8.1）** — 「配布テンプレートのサンプル行を空欄化」はテンプレート固有の対処。外部勤怠は取込元CSVにコード列がある場合の扱いが未定義
  - 複数店舗のマッピング（§8.3）／営業時間の該当列（§7.3）／§2 全体の §1 への追随
- **モックの外部勤怠画面18枚に「後続フェーズ・参照用」バナーを表示**（`renderPhaseBanners()`）。画面は撤去せず、先行実装の対象外であることを画面上でも示す。テンプレート側11画面には表示しない。
- jsdom で検証（外部勤怠18/18にバナー・テンプレ11画面には非表示・動的生成画面にも付与・既存機能の回帰）。全項目通過。

### Changed（実装フェーズを明記 — テンプレート先行 / 外部勤怠は後続）
- **テンプレートインポートを先行実装、外部勤怠サービスインポートを後続実装と確定**し、「インポート方式」の表に実装フェーズ列を追加。§2 の冒頭にも後続である旨を明記した。
- **仕様の正本はテンプレートインポート側（§1）**。§7〜§9 の決定事項はまずテンプレート側に適用し、外部勤怠側のマッピング表は後続フェーズで §1 に追随させる（先行フェーズでは凍結）。モックも先行実装の対象は `s-tpl*` 系のみで、`s-saas*` 系は後続フェーズの参照用と位置づけた。
- **「ラクミー側の許可値は実装（DBのenum）を正とする」を運用ルールとして明記。** 外部サービスの語彙をそのまま許可値に写さない。給与体系の日給（§8.10）は、KoT人事労務の `個人.契約種別` と Jobcan給与計算の `給与形態` が取りうる値をそのまま採用したもので、初版（2026-03-31）から混入していた。マッピング表が外部サービス起点で書かれているため「外部サービス側の値」と「ラクミー側の許可値」を別物として扱う。

### Changed（エンジニア共有資料を1本に統合）
- `20260728_エンジニア質問_回答と対応.md` を**共有用の正本**とし、内容が重複していた `20260728_仕様更新_差分サマリ.md` を**削除**。2本あると「どちらが最新か」で迷い、片方だけ更新される事故につながるため。
- 削除前に、差分サマリにしかなかった情報（§9 各状態のモック実装識別子、行数上限の画面別内訳と受付拒否の文言、動的注入方式の説明）を回答書へ吸収。
- **Q1の回答が仕様書に記録されていなかったため §7.2 に追記**。①〜③と④⑤でタブの軸が違うこと（種別切替 / 行ステータス）、①〜③にステータスタブを足さない理由、①〜③のエラーはバナー＋行内3択で表示すること、④⑤のタブは実装では実フィルタにすることを明記。チャットで回答したまま仕様書に落としておらず、仕様書だけを見たエンジニアには伝わらない状態だった。

### Fixed（仕様書とモックの不整合 4件 — 再監査）
- **仕様書の記載が事実と違っていたのを訂正**: §9.2 と本CHANGELOGに「完了画面4箇所に実装」と書いていたが、「次にやること」は実際には `s-tpl3` / `s-saas4` の**2箇所しか無かった**。件数を数えずに記載していた。`renderNextStepsBlocks()` による注入に変更し、単体取込の完了画面2つを含む**4画面すべて**に配るよう修正。
- **自動採番の注記が `s-tpl2` の1画面のみ**で、全画面横展開の方針と矛盾していた。`renderAutoCodeNotes()` による注入に変更し、プレビュー/確認画面**4画面すべて**に配布。
- **④従業員の単体取込画面で「500行」のエラー文言が出ていた**。デモバーを全画面に配った結果、種別が確定している画面でも一括画面用の併記文言（500行＋※④は200行）が表示されていた。`activeEntityKey()` を追加し、④従業員・補完テンプレートは200行、①②③⑤は500行、一括画面のみ併記、と**画面の取込対象種別で出し分け**るよう修正。
- **失敗行CSVの拡張子が仕様(.csv)とモック(.tsv)で食い違っていた**。テンプレートがタブ区切りである実態に合わせ、仕様側を `.tsv` に統一。ファイル名も `{元テンプレート名}_失敗行_{YYYYMMDD}.tsv` に揃えた。
- jsdom で19項目を検証（次にやること4箇所・採番注記4箇所・行数超過文言の4パターン出し分け・CSVファイル名と中身・既存機能の回帰）。全項目通過。

### Added（プレビューで除外した行を完了画面で開示 — README §9.7）
- **エラーの発生タイミングを3種類に整理**：①アップロード時(受付拒否)／②プレビュー時(値不正・退職者・担当店舗外)／③実行時(429・キー衝突)。
- **②で除外した行が完了画面から消えていた**問題を修正。15行中1行がエラーだと「14件すべて成功」とだけ表示され、利用者が除外分を放置する状態だった。③には失敗行テーブルとCSVがあるのに②には回収動線が無く、テンプレ側(`s-tpl3`)と外部勤怠側(`s-saas4`)で扱いも揃っていなかった。
- **完了画面4箇所に「取り込まなかった行 (N件) — プレビューでエラーのため除外」を常時表示**（`renderExcludedRowsBlocks()`）。全件成功時も表示する。
- **失敗行CSVに除外分を含める**。失敗理由列に `プレビューでエラーのため除外: {理由}` を記載し、実行時の失敗と同じファイルで回収できるようにした。
- ①のアップロード時エラーは完了画面に出さない（ファイルが受理されておらず取り込みセッションが始まっていないため）。

### Fixed（受付拒否バナーが押しても反応しないように見える問題）
- バナーを `.content` の**先頭**に挿入していたため、アップロード欄までスクロールした状態では画面外に出て、ボタンが機能していないように見えていた。**デモバーの直前（＝アップロード欄の直下）に挿入**するよう変更。
- あわせて `s-tpl1` のインライン5ボタンを共通プレースホルダ（`.upload-reject-demo`）に統一し、全画面で同一の仕組みに。
- 0件デモ時に**サマリーボックスの数値も連動**するよう修正（エラー0件のまま「取り込めるデータが1件もありません」と出て矛盾して見えていた）。
- jsdom によるDOMテストで、バナー生成・挿入位置・閉じる・画面遷移での持ち越し・0件時のボタン不活性とサマリ連動・ロール切替・CSV内容を検証（全項目通過）。

### Fixed（仕様書とモックの整合性を回復 — 7件）
- コミット `20a1fb5` の内容を仕様書と突き合わせ、**仕様書に書いたがモックに無い／数値が食い違う 7件を修正**。
- **§7.5 の見出しが「全テンプレート一律 500行」のままで、同じ節の追記(④のみ200行)と自己矛盾していた**のを修正。見出しを「①②③⑤は500行、④従業員は200行」に。
- **モックのアップロード表記7箇所が「最大500行」のまま**で、④従業員の画面でも500行と表示されていた。`rowLimitOf()` を追加し種別ごとに出し分け。一括・外部勤怠画面は「500行／④従業員は200行」を併記、補完画面(④のみ対象)は200行に。
- §8.1 の自動採番形式とモック表示(`EMP-001`)の不一致 → プレビュー表に「表示されるコードは自動採番された値」の注記を追加（26セルの書き換えはテーブル幅を圧迫するため見送り）。
- §8.3 で「完了画面の次にやることに追加する」と決定しながら未実装だった案内を、**完了画面4箇所に実装**。
- §4.3 で確定した「空欄セルは変更しない」がUIに出ていなかったため、**「重複時の処理」UI 3箇所すべてに注記を追加**。
- §8.9(退職者)・§9.6(担当店舗外)のエラー文言がモックに1件も無かったため、④従業員プレビューに該当行を追加（`data-err-type="retired"` / `"out-of-scope"`）。

### Changed（§9 の状態を代表画面から全画面へ横展開）
- 受付拒否バナー(§9.3・§9.5)と0件状態(§9.4)を、**アクティブ画面へ動的注入する共通実装**に変更。静的画面・ウィザード・動的生成される単体取込画面のいずれでも同じ挙動になる。
- デモ操作列は `.upload-reject-demo` プレースホルダ(4箇所)と `[data-import-exec]` 属性(4ボタン)を持つ画面すべてに自動配布（`renderUploadRejectDemoBars()` / `renderZeroTargetDemoBars()`）。`go()` から再描画し、画面遷移時にバナーを持ち越さない。

### Added（実装との突き合わせで10件を確定 — README §8）
- テンプレート列定義を `rakmy/rakmytimerecord`（`origin/feature/189`）と照合し、未確定・実装と矛盾する**10件を §8 として起票し全件確定**。エンジニア共有用の要約は `20260728_エンジニア質問_回答と対応.md`。
- **§8.1 コードのUNIQUEが全社横断**: 突合は必ず `companyId` で絞る（§4.3 に明記）。**配布テンプレのサンプル行のコードを空欄化**（従来は `EMP-001` 等が入っており2社目以降が必ず衝突した）。自動採番規則 `EMP-{companyId先頭8文字}-{連番4桁}` を定義。
- **§8.2 従業員1件の取込に最大3リクエスト**: `createEmployeeSchema` に交通費フィールドが無く専用エンドポイントが別にある。§7.5 の上限根拠が実態と乖離していたため **④従業員のみ上限を200行に変更**（他は500行）。
- 他8件: 複数店舗は単一のまま（④⑤の非対称を意図的と確定）／月みなし残業時間は `shift_restriction`（text）にマッピングし0〜999の整数検証／業態名・店舗名は会社内完全一致・DB側同名複数はエラー／雇用区分キーはコード優先・空欄時のみ名前／**CSVの空欄セルは「変更しない」（NULL上書きしない）**／列バリデーションはAPIスキーマ側の実効上限を採用（従業員名は100）・金額と電話番号は正規化／退職者と同コードはエラー（自動復活させない）／日給を削除。

### Added（状態遷移6件を定義しモックに実装 — README §9）
- モックは正常系と行レベルのエラーを網羅していたが、以下が存在しなかったため**定義＋実装**。
- **§9.1 取り込み実行中**（`#import-running-overlay` / `runImport()`）: 進捗もスピナーも無く、**実行ボタンの二重押下防止も無かった**（既存 `disabled` は必須項目未達の制御のみ）。オーバーレイ＋プログレスバー＋`beforeunload` 警告＋`_importRunning` フラグを追加。
- **§9.2 失敗行CSVダウンロード**（`downloadFailedRowsCsv()` / 完了画面4箇所）: 「失敗した1件を再取り込み →」がアップロード画面の先頭に戻すだけで、失敗行だけのCSVを作る手段が無かった。**§7.7 の「再取り込みで回収できます」がこの動線なしでは成立しない**ため追加。元テンプレ同一列＋末尾に失敗理由列。
- **§9.3 上限超過の受付拒否**（`#tpl-upload-reject`）: 従来は仕様メモ枠にエラー文言が静的に置かれているだけで、遷移が無かった。
- **§9.4 全件エラー0件**（`demoZeroTarget()`）: 実行ボタンを `disabled` にし `（0件）` を表示。
- **§9.5 ファイル形式エラー**（`UPLOAD_REJECT_MSG`）: 文字コード／列名不一致／データ0件。テンプレDLは**BOM付きUTF-8で配布**と決定し、テンプレ側に文字コード選択UIは設けない。
- **§9.6 権限による出し分け**（`setHubRole()`）: 管理者にも開放するが**全社マスタ（①②③）はオーナー限定**、④⑤は担当店舗の行のみ。

### Changed（給与体系から日給を削除 — README §8.10）
- モック・テンプレート・入力ガイド・マッピング表の3値（月給/日給/時給）を**2値（月給/時給）に統一**。アプリ側は `pgEnum("employment_category", ["時給","月給"])` の2値で、`profile/route.ts` の else 分岐により日給を足すとサイレントに月給として計算されるため。
- `③雇用区分.tsv` のサンプル行 `日給スタッフC / 日給 / 10000 / 8` → `アルバイトC / 時給 / 1100 / 空欄`（末尾の `8` は月次契約労働時間の列に1日の所定労働時間を誤って置いたもの）。`④従業員.tsv` の雇用区分名も追随。
- **経営管理側のマッピング先（`給与.日給月給制` / `給与.日給制`）は変更していない**。経営管理のフィールド名であり、日給月給制は月給の一種で日給ではない。

### Fixed（モックのコメントと確定仕様の不整合を解消）
- `index.html` の `demoResult()` コメントが「ロールバックできず一部だけ登録された状態」と記載され、UI確定文言「失敗した行はDBに残っていません（ロールバック済み）」と矛盾していた。§7.7 の粒度（バッチは巻き戻さない／失敗行は補償削除）に合わせて修正。

## 2026-07-16

### Fixed（単体取込フローの戻り先を入場経路で全面出し分け）
- **入場経路で戻り先を分岐**（`_singleEntryFrom`＝`hub`/`settings`）：
  - アップロード画面の**「閉じる」**（`singleClose`）：ハブ経由→**TOP（`s-top`）**、設定画面の📥経由（`?type=&mode=`）→**元の設定画面**（`returnToSourceSettingByKey`）。
  - **完了画面の戻る**（`singleCompleteReturn`＋`updateSingleCompleteReturn`）：ハブ経由→ボタン「**← インポートTOPに戻る**」で `s-top`、設定画面経由→「**← 設定画面に戻る**」で `returnToSourceSetting`。ガイド文も経路で出し分け。
  - 従来は常に owner-mock 設定画面へ飛び、ハブ経由でも違う画面へ遷移していた不具合を是正。

### Changed（単体取込画面の作業ステップ表記を撤去＝旧仕様の整理）
- **番号付きプログレスバー（連携元→アップロード→プレビュー→完了）を撤去**：運用の単体取込は連携元・取込対象が確定済みで実作業はアップロードのみのため、連番の作業ステップ表示をやめた。
- 各カード見出しの**連番（1/2/3）も削除**（「取込対象」「ファイルをアップロード」）。連携元は素の1行「設定済み：{サービス名}」。

### Changed（インポートTOP＝運用ハブに刷新・入口分離）
- **左ナビ「インポート」＝運用インポートのハブ**（`s-top`）に。種別（業態/店舗/雇用区分/従業員/管理者）ごとの単体取込入口。
- **初期セットアップの一括取込（`s-tpl1`/`s-saas1`）はTOPから撤去**し、オンボーディング b0（`#tpl-setup`/`#saas-setup`）専用に限定。初期（一括）と運用（単体）の入口・表示を明確化。
- **インポート方式（テンプレ/外部勤怠）は初期設定で決定済みのグローバル設定**：運用では毎回選ばせず、現在方式を上部ステータス表示。種別「取り込み →」は現在方式で直接単体取込へ（`hubGoEntity`→`s-{tpl|saas}-single-X`）。
- **右上「⚙ インポート方式を再設定」**：方式を選び直すスライド（`hubReconfig`/`hubSetMethod`）。テンプレ/外部勤怠の2カード、現在方式に「現在の設定」バッジ、`localStorage` 保持。
- **外部勤怠選択時は連携元サービス選択スライドを1枚追加**（キングオブタイム/ジョブカン/その他・`hubSetService`／初期セットアップと同方式）。現在方式は**「🔄 外部勤怠サービス: {サービス名}」**と連携元まで明示。選び終えると種別一覧へ、種別クリックで `s-saas-single-X` へ（`_activeSaasSource` 同期／`jc`→`jobcan` マッピング）。
- **単体取込画面（s-saas-single-X）の連携元「再選択」を廃止**：ハブで選択済みのため、Step1 を選択UI→**読み取り専用表示「連携元サービス（設定済み）: {サービス名}」**に（`applyToSingleScreen` で `saas-src-cards` を隠し `saas-src-ro` を表示）。変更はTOPの「方式を再設定」から。二重の連携元選択を解消。

## 2026-07-13

### Changed（外部勤怠サービス取込 s-saas1 UI）
- **連携元サービス選択 → 取り込みステップへスライド**：サービス（KOT/JC/その他）を選ぶとピッカーが退場し、そのサービス専用の取り込みウィザードへスライド表示（`saasSyncSlideView` / `saasPickerConfirmed`）。「連携元サービスを変更」で選択へ戻れる。
- **選択中サービス名を左上に明示**：「取り込むデータを順番に」の上に `連携元サービス [ロゴ] サービス名` バーを表示（何に対するアップロードか明確化）。その他は取り込みステップの代わりに問い合わせ案内。
- **「取り込み設定」ボタンを「取り込むデータを順番に」カードの右上へ移設**（フッターの btn-group から移動）。

### Changed（外部勤怠取込 s-saas1 — 完了スライド）※2026-07-16
- **全アップロード完了時はスライドを「完了」表示に畳む**：個別のDL/アップUIの代わりに「✅ すべてのデータをアップロード完了」バナーを表示（取り込み対象一覧・戻るは維持）。**確認画面への遷移は下部フッターの「確認画面へ →」のみ**（スライド内には確認ボタンを置かない＝PdM選択）。冗長な下部ヒントは撤去。

### Changed（外部勤怠取込 s-saas1 / 取り込み設定）
- **SaaSの完了モーダルを廃止**：外部勤怠は必須/任意が混在するため、テンプレ取込のような「必須完了モーダル」は出さない（トリガー・デモボタン・モーダル本体を撤去）。
- **「取り込み対象」サマリを追加**（アップロード欄の下）：**アップ済み（=取り込み対象）だけを育てて明示**する方式。全項目を常時 `○` で並べる案は上部ステップドットと重複するため不採用。
  - **視覚的に“従”へ調整**：当ステップの「アップロード」操作より目立たないよう、緑ヘッダー＋警告バーの主張の強い箱をやめ、控えめなグレーの小サマリに。**0件時は1行に圧縮**（必須未アップのみ小さく赤字）。アップ済みは `✓ 項目名 … ファイル名` を淡色で列挙、残りの任意は件数のみ案内。
- **ステップ2の見出しを「アップロード」に簡素化**（旧「記入してアップロード」「エクスポートしてアップロード」）。テンプレ取込 s-tpl1 も同様。手順詳細は各項目の hint に集約。
- **「マッピングを編集」をテキストリンク→明示ボタン化**（取り込み設定 s-saas-settings の各データソース）。枠付き青ボタン＋✏️アイコンで視認性向上。

### Changed（両ウィザードの「次へ / 確認画面へ」の整理 — s-tpl1 / s-saas1）
- **確認画面(プレビュー)への遷移をフッターの1ボタンに一本化**：カード内の「次へ」は“次のアップロード項目へ進む”用途のみに限定。**全アップロード完了時（または最終項目）はカード内の「次へ」を非表示**（戻る・ステップ直接クリックは可能）。確認画面へは**枠外フッターの「確認画面へ →」（tpl）/「次へ: プレビュー →」（saas）のみ**で遷移。フッターボタンは**必須アップ完了で活性化**。
  - ※ 以前「フッター側を撤去（カードに一本化）」としたが、PdM 指示により**逆（フッターに一本化・カードの確認ボタン撤去）**へ最終決定。
  - 全完了時はカード下部に「✓ すべてアップロード済み — 下部の『確認画面へ →』で進めます」を表示。

### Changed（テンプレ取込ウィザード s-tpl1）
- **管理者(任意)アップ完了モーダル(B)を追加**：必須4項目アップ完了モーダル(A)で「⑤ 管理者(任意)も取り込む」を選び、管理者テンプレDL→アップロードを終えると、**「必須＋任意データのアップロードが完了しました」モーダル**を表示し、その場の「→ このまま確認画面へ進む」で s-tpl2 へ遷移（`showTplAdminDoneModal` / `tplAdminDoneGoConfirm`）。

### Changed（完了系UIの整理）
- **アップロード完了モーダルと最終「インポート完了」画面の役割を明確化**：中間モーダル（必須アップ完了時）の見出しを「取り込みが完了」→**「アップロードが完了」**に変更し、「実際の取り込み（登録）は確認画面で実行し、完了画面に結果表示」と補記。→ 2つの完了表示（中間モーダル＝アップ完了の案内 / s-tpl3・s-saas4＝実行後の結果）が別物と分かるように。
- **「インポート完了」画面をモーダル幅→画面幅に拡大**：`.result-detail max-width:360px`→フル（`.result-center` を最大680px中央寄せ）、アイコン/見出し/行の文字サイズを拡大（56→64 / 18→24 / 13→14px）。s-tpl3・s-saas4 共通。

### Fixed
- **中断ラベルの文言是正**：一時保存ドラフトの `label` を「✓ 必須項目すべて完了(登録前準備完了)」→ **「必須ファイルをすべてアップ済み（本取り込み前）」**に。打刻記録の再開バナーで「完了」と誤読される問題を解消（owner-mock 側バナーと整合）。
- **「初期セットアップへ戻る」ボタンの遷移を確実化**（`bGoBackToSetupGuide`）：親タブ(初期セットアップ)から開かれた場合は `window.opener`（同一オリジン）で親を `#b0` に切り替えて前面化＋自タブを閉じる。単独タブ時はこの画面を `#b0` へ遷移。旧実装は `window.close()` 依存で単独タブ時に不発だった。
- **未アップロード時は「一時保存」「一時保存して中断」を押下不可に**（テンプレ／外部勤怠サービス両ウィザード s-tpl1 / s-saas1）。
  - 判定を新ウィザードのアップロード有無（`wizHasAnyUpload`）に一本化。`syncWizSaveButtons()` を `renderTplWizard()`/`renderSaasWizard()` 末尾で呼び、フッターの一時保存ボタン＋page-header の中断ボタンを同期。
  - 旧 `refreshSaveButtonsState` は legacy チェックボックス（ウィザードでは非表示）で判定しており、フッターだけ有効化される不整合があったのを是正。1件でもアップロードすると両ボタン有効化。

---

## 2026-06-24

### Changed（owner-mock 決定 #13/#17 に追従）
- **業態・ブランド → 業態に統合**：ブランド／ブランドコード廃止。
  - ①テンプレを 3列（業態/ブランド名/ブランドコード）→ **1列（業態名）**、ファイル名も `①業態.tsv` に。
  - ②店舗の `所属ブランドコード` → `所属業態`。
  - 取込プレビュー・マッピング（`brand_code` 削除）・入力ガイド・README・物理TSV から コード列を除去。
- **業態名の入力形式**：入力ガイドで「選択」→「テキスト」に修正（名前付けの自由入力マスタ）。
- **`brand` → `businesstype` に一括改称**：画面ID・関数・データキー（`s-tpl-single-businesstype` / `downloadBusinesstypeTemplate` / `map-kot-businesstype` ほか）、下書きスペックTSVの参照IDも追従。

---

## 〜2026-06-04（ベースライン）

- 旧「2テンプレート方式」→ **5テンプレート方式（エンティティ別）** に統一（README・実ファイル・モック画面）。
- native alert を `showFloatToast`（トースト）に全置換。
- 到達不能の孤立画面 `s-saas3`（旧プレビュー）撤去。
- 一括取込プレビュー/完了画面の旧ラベルを5テンプレ体系に統一。
- README に owner-mock を入口とする導線を追加。
