# 受け入れ条件の使い方（定義表・確認表）

**作成日**: 2026-08-04

> **誰が読むか**: 実装するエンジニア／検証する人／リリースを判断する人
> **対象**: `20260804_受け入れ条件_定義表.tsv` と `20260804_受け入れ条件_確認表.tsv`

---

## 1. 資料の関係

**受け入れ条件は2回使います。実装前は「約束」、実装後は「判定」です。**

```
  ① 仕様書        なぜそう作るか（設計意図・境界）
      ↓
  ② 受入条件        何ができたら完成か（合格ライン）  ← 実装前に合意する
      ↓
  ③ 検証プラン      期待どおり動くか（操作と期待値）  ← 実測値を出す
      ↓
  ② 受入条件        実測値が合格ラインを満たすか      ← リリース判断
```

| 資料 | 何のためか | いつ使うか |
|---|---|---|
| **定義表** | 何を守るか・なぜか | 実装前の合意／設計レビュー |
| **確認表** | 実装が条件に合致しているかの判定 | 実装後・検証後 |
| 検証プラン | 操作して実測値を出す | 検証の作業中 |

**定義表は書き換えません。** 仕様が変わったときだけ直します。
**確認表に書き込みます。** 実装状況・検証結果・判定を記入します。

---

## 2. 定義表の読み方

| 列 | 意味 |
|---|---|
| **設計ポイント** | 何を守る条件か。①〜⑬。**上にあるほど崩れたときの損害が大きい** |
| 条件ID | `AC-001` のような一意の番号。確認表・トレーサビリティ表と対応する |
| マイルストーン | どの実装段階で満たすか（M1〜M6） |
| **満たすべきこと** | 実装の到達点。1文 |
| **MVP区分** | 実装の優先度。後述 |
| **崩れると起きること** | **なぜこの条件があるか。実装の優先度はここで判断する** |
| 仕様の該当箇所 | 詳細を読むところ |

### MVP区分の意味

| 区分 | 意味 | 件数 |
|---|---|---|
| **MVP必須** | **これが無いと機能として成立しない。** データが壊れる／取り返しがつかない／二重登録される／導入が完了しない | **34件** |
| リリース必須 | 顧客に出すのに必要。無いと問い合わせや誤解が起きる | 26件 |
| 改善 | 無くても運用は回る。余力があれば | 9件 |
| — | 対象外・検証の前提（実装対象ではない） | 5件 |

**エンジニアはMVP必須の34件を先に潰してください。** ここが埋まらない状態でリリース判断はできません。

---

## 3. 確認表の書き方

### 列の意味

| 列 | 誰が埋めるか | 内容 |
|---|---|---|
| 条件ID 〜 満たすべきこと | 記入不要 | 定義表と同じ |
| **実装箇所（画面・機能）** | 記入不要 | どこを見れば実装が確認できるか |
| **合致していると言える状態** | 記入不要 | **合格ライン。すべて数で書いてある** |
| **確認方法** | 記入不要 | 動作確認／動作確認＋DB確認／実装レビュー |
| **根拠（検証ID）** | 記入不要 | 検証プランのどの項目を見るか |
| **実装状況** | エンジニア | `○` 実装済み ／ `△` 一部 ／ `×` 未実装 |
| **検証結果** | 検証する人 | 検証プランで出た実測値を書く |
| **判定** | 判断する人 | `合格` ／ `不合格` |
| 確認日 | 判断する人 | 確認した日 |
| メモ | 誰でも | 不合格の理由、回避策など |

### 記入の例

**AC-053「昇格で勤務店舗が消えない」の場合**

記入前

| 列 | 内容 |
|---|---|
| 合致していると言える状態 | 元の2店舗の所属が維持されている（消えた件数＝0件） |
| 確認方法 | 動作確認（検証プラン） |
| 根拠（検証ID） | OP-607 / CA-061 |
| 実装状況 | （空） |
| 検証結果 | （空） |
| 判定 | （空） |

記入後

| 列 | 内容 |
|---|---|
| 実装状況 | ○ |
| 検証結果 | 2店舗所属の従業員を渋谷店のみで昇格 → 担当店舗2件のまま |
| 判定 | 合格 |
| 確認日 | 2026-08-15 |

**合格ラインが数で書いてあるので、実測値と並べれば判定できます。**

---

## 4. 進め方

### 実装前（〜8/14）

1. **定義表のMVP必須34件をエンジニアと確認する** — 「これが完成の条件です」と合意する
2. マイルストーン表（M1〜M6）で実装の順序を決める
3. **異論があればこの時点で出す。** 実装後に条件を変えると判定の意味がなくなる

### 実装後（8/14）

4. エンジニアが**確認表の `実装状況`** に `○△×` を記入する
5. `×` や `△` があれば、**検証しても不合格になる**。実装状況を先に埋めることで、未実装と不具合を区別できる

### 検証（8/15〜）

6. **検証プランを段階順（P0→P9）に実施する** — 確認表ではなく検証プランを見て操作する
7. 検証プランの `実際の結果` `判定` を記入する
8. その結果を**確認表の `検証結果`** に転記する

### 判定（8/16）

9. 確認表の `判定` に `合格` / `不合格` を記入する
10. `python3 progress.py` でマイルストーン別の達成度とリリース可否が出る

---

## 5. 確認方法の3種類

**すべてが動作で確認できるわけではありません。**

| 確認方法 | 件数 | 何をするか |
|---|---|---|
| **動作確認（検証プラン）** | 62 | 検証プランの手順どおりに操作し、結果を見る |
| **動作確認＋DB確認** | 3 | 画面だけでは分からない。DBを直接見る |
| **実装レビュー** | 9 | 動かしても分からない。**成果物そのものを開いて突き合わせる** |

### 動作確認＋DB確認（3件）

| 条件ID | なぜDBを見るか |
|---|---|
| AC-044 失敗した行がDBに残らない | 画面には出ない。**失敗行のレコードが0件であることをDBで確認する** |
| AC-052 コードが自動採番される | 全社横断でユニークかは他社データを含めて見る必要がある |
| AC-051 テンプレートの全列が反映される | 画面に出ない列があるため |

### 実装レビュー（9件）

| 条件ID | 何を見るか |
|---|---|
| AC-002 / AC-003 / AC-004 | **配布テンプレートのファイルを開いて列を数える** |
| AC-058 登録経路によるスキーマの差異が無い | 手入力の登録モーダルとCSVテンプレートの項目を並べて突き合わせる |
| AC-081 / AC-082 | 対象外の機能が実装対象に含まれていないことを確認する |
| AC-005 / AC-007 / AC-008 | 検証の前提が整っていることを確認する |

**これらは検証プランを経由しません。** 動作では確認できないためです。

---

## 6. リリース判断のルール

| 状態 | 判断 |
|---|---|
| **MVP必須34件がすべて合格** | **リリース可** |
| MVP必須に不合格がある | **リリース不可。** 修正して再検証 |
| リリース必須に不合格がある | 影響を確認して判断。**回避策を案内できるなら可** |
| 改善のみ不合格 | リリース可。次回に回す |

### 集計する

```
python3 progress.py
```

```
  M1 配布物とアップロードの入口   MVP必須  6/6  ██████       完了
  M2 プレビューと判定          MVP必須  7/10 ███████···   着手
  ...
  リリース不可。MVP必須に不合格が 2 件あります
    AC-044 AC-053
```

**不合格があると「崩れると起きること」も表示されます。** 影響を見てから判断できます。

---

## 7. 迷ったとき

### 未実装なのか不具合なのか分からない

**`実装状況` を先に埋めてください。** `×`（未実装）なら不具合ではありません。8/14に実装状況を確認してから検証に入る順序にしているのは、これを区別するためです。

### 一部だけ満たしている

**`実装状況` は `△`、`判定` は `不合格`** にします。合格ラインは「〜＝0件」の形なので、部分的な達成は合格になりません。**メモに何が足りないかを書いてください。**

### 1つの検証項目が複数の条件の根拠になっている

**22件あります。** 1回の操作で複数の条件を判定できます。たとえば OP-503（失敗行CSV）は AC-043・AC-044・AC-046 の根拠です。**検証は1回で済みますが、判定は条件ごとに行います。**

### 合格ラインが実装と食い違う

**実装を直すのが原則です。** 条件は実装前に合意したものだからです。ただし仕様の誤りが判明した場合は、**定義表と仕様書の両方を直し、変更した理由を `JUDGMENT_LOG.md` に残してください。** 確認表だけ直すと、次回に同じ議論が起きます。

---

## 8. スプレッドシートで使う場合

**`.tsv` をそのままアップロードできます。** ファイル → インポート → アップロード → 区切り文字は「自動的に検出する」。

推奨する入力規則

| シート | 列 | 選択肢 |
|---|---|---|
| 確認表 | 実装状況 | `○` `△` `×` |
| 確認表 | 判定 | `合格` `不合格` |
| 検証プラン | 判定 | `合格` `不合格` |

**`progress.py` はローカルのTSVを読みます。** スプレッドシートで記入した場合、集計は連動しません。スプレッドシート側で集計する場合は次の数式が使えます。

```
MVP必須の合格数    =COUNTIFS(確認表!B:B,"MVP必須",確認表!K:K,"合格")
MVP必須の総数      =COUNTIF(確認表!B:B,"MVP必須")
M1の達成度         =COUNTIFS(確認表!C:C,"M1",確認表!B:B,"MVP必須",確認表!K:K,"合格")
```

---

## 9. 関連資料

| ファイル | 内容 |
|---|---|
| `20260804_受け入れ条件_定義表.tsv` | 何を守るか・なぜか（74件） |
| `20260804_受け入れ条件_確認表.tsv` | 実装が条件に合致しているかの判定（74件） |
| `20260804_実装マイルストーン表.tsv` | M1〜M6の実装順序と完了条件 |
| `20260803_検証プラン_操作系.tsv` | 操作する検証（53件） |
| `20260803_検証プラン_計算系.tsv` | 数値・上限の検証（19件） |
| `20260803_検証用データセット.tsv` | 検証に使うデータ（34件） |
| `20260803_CSV一括登録機能_仕様書（PdM向け）.md` | 設計意図。§8.2 に判断基準 |
| `progress.py` | 達成度とリリース可否を出す |

**画面モック**

| 経路 | URL |
|---|---|
| CSVインポート | https://suguru789987.github.io/rakmy-timerecord-import-spec/ |
| 手入力（登録・編集・削除） | https://suguru789987.github.io/rakmy-owner-registration-mock/ |
