# プロンプト集（Claude Code で使う指示の例）

このリポジトリのフォルダで Claude Code を開き、そのまま貼って使えます。
**成果物のURLは `成果物URL一覧.md` にあります。**

---

## 1. 最初に1回だけ

### 受け取った直後

```
このリポジトリはラクミー タイムレコードのCSV一括登録機能の仕様一式です。
まず python3 check_all.py を実行して、資料に食い違いが無いか確認してください。
何か出たら内容を要約して、どう直せばよいか教えてください。
```

### 全体像をつかむ

```
このリポジトリの成果物5点（仕様書・受け入れ条件表・検証プラン・検証データセット・
ヘルプページ）が、それぞれ何を決めていて、どう繋がっているかを説明してください。
HANDOFF.md と 仕様書の §8 を読んでから答えてください。
```

---

## 2. 成果物を見る

### URLを出してもらう

```
以下の成果物のURLを出してください。
- 仕様書
- 受け入れ条件表
- 検証プラン
- 検証データセット
- ヘルプページ
```

> `成果物URL一覧.md` に載っています。**Claude Code はこのファイルを読んで答えます。**

### 中身を要約してもらう

```
仕様書（PdM向け）の §8「検証の範囲とリリース判断」を要約してください。
```

```
受け入れ条件のうち MVP必須のものだけを、マイルストーン順に一覧にしてください。
```

```
ヘルプページの掲載本文の目次を出して、どの読み手が何番を読むのか説明してください。
```

---

## 3. 実装のとき

### 何から作るか

```
実装マイルストーン表と受け入れ条件表を見て、M1で作るものと、
その完了をどう判定するかを教えてください。
```

### ある条件の意味を知る

```
AC-035 について、何を実装すればよいか、満たさないと何が起きるか、
どの検証で確かめるかを教えてください。
```

### 仕様の根拠をたどる

```
従業員コードの自動採番について、仕様書・README・受け入れ条件・検証プランの
どこに何が書いてあるかを対応づけて示してください。
```

---

## 4. 検証のとき

### 準備するもの

```
検証を始める前に用意すべき前提データ（D0）を一覧にして、
それぞれの作り方を教えてください。
```

### 1件ずつ実施する

```
OP-211 の検証手順を教えてください。
どの画面を開き、何を操作し、どうなれば合格で、何を証跡に残すかまで。
```

### 結果を記録する

```
OP-211 を実施して期待どおりでした。
受け入れ条件のどれが合格になるか教えてください。
```

```
CA-016 が不合格でした。実測値は「0が登録できてしまう」です。
影響する受け入れ条件と、ヘルプページで直すべき箇所を教えてください。
```

### 進み具合を見る

```
python3 progress.py を実行して、マイルストーンごとの達成度と
リリース可否を教えてください。
```

---

## 5. 資料を直すとき

### 直したあとに必ず流す

```
資料を直したので、python3 sync_docs.py → sync_excel.py → check_all.py を
順に実行して、「整合しています」と出るまで直してください。
```

> **数字を手で書き換えないでください。** TSVを直して上の3つを流せば、仕様書もエクセルも追随します。

### 受け入れ条件を1件足す

```
受け入れ条件を1件追加したいです。内容は「〇〇」です。
定義表と確認表の両方に足し、対応する検証プランとヘルプの紐付けも入れて、
最後に check_all.py で確認してください。
```

### 仕様が変わったとき

```
検証の結果、〇〇の仕様を変えることになりました。
影響する受け入れ条件・検証プラン・ヘルプページの箇所をすべて挙げてから、
直してください。
```

> **検証プランの「ヘルプ該当箇所」列**に、直すべきヘルプの節と小見出しが書いてあります。

---

## 6. ヘルプページを公開するとき

```
ヘルプページを公開したいです。
作業用ファイルの「公開するときの手順」と「掲載前のチェックリスト」を読んで、
いま何が残っているか教えてください。
```

> **CSVインポートと初期セットアップが実装・検証を通るまで公開しません。** 公開時は既存ヘルプページも同時に直します。

---

## 7. 困ったとき

### 何が起きているか分からない

```
python3 check_all.py の結果を見て、何が問題で、どう直せばよいか、
優先度をつけて教えてください。
```

### 過去の判断を調べる

```
〇〇について、過去にどう判断したか JUDGMENT_LOG.md から探してください。
```

### 未解決のものを知る

```
HANDOFF.md と仕様書から、まだ決まっていないこと・確認が必要なことを
すべて挙げてください。
```

---

## 覚えておく3つのコマンド

```
python3 sync_docs.py    # 仕様書の件数・列数とURL一覧を書き直す
python3 sync_excel.py   # 配布用エクセルを作り直す
python3 check_all.py    # 全資料の整合を検査（終了コード0なら整合）
```

**`check_all.py` が「整合しています」と出れば、資料間の食い違いはありません。**
ただし**条件どうしの意味の矛盾**（ある条件が別の条件の前提を壊す等）は、
気づいたときに `前提となる条件` 列へ書き足してください。以後は機械が守ります。
