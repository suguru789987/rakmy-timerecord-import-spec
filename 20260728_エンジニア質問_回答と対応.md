# エンジニア質問への回答と対応（2026-07-28）

タイムレコード CSV取込機能。いただいた確認事項3件への回答と、それぞれに対して**仕様書・モックをどう変更したか**をまとめています。

**照合基準ブランチ: `rakmy/rakmytimerecord` の `origin/feature/189`**（最終コミット 2026-07-10）
`main` / `develop` には未マージです。`main` と照合すると §7.3 が誤りに見えるため、確認時は必ずブランチを合わせてください。

## 実装スコープ

**先行実装は「テンプレートインポート」です。外部勤怠サービスインポート（KoT/Jobcan）は後続実装。**

- 仕様の正本はテンプレートインポート側（README §1）。以下の回答・決定事項は、まずテンプレートインポートに適用してください
- モックには両方式の画面がありますが、**先行実装の対象は `s-tpl*` 系の画面のみ**です。`s-saas*` 系は後続フェーズの参照用
- 外部勤怠側のマッピング表は後続フェーズで §1 の確定内容に追随させます。先行フェーズでは凍結します

**ラクミー側の許可値は、実装（DBのenum）を正とします。** 外部サービスの語彙をそのまま許可値に写しません。Q2の日給がまさにこの取り違えでした（後述）。

**各決定事項には「適用範囲」を明記しました。** README の §7.1〜§9.7 の各項目冒頭に、次のいずれかが入っています。

- `両方式共通` … DB制約・API仕様・画面挙動に起因するもの
- `テンプレートのみ` … テンプレートの列定義やファイル配布に起因するもの
- `方式別に定義済み` … 方式ごとに要件が異なり、それぞれ定義したもの（§9.5 文字コードの扱い）

**外部勤怠で未定義の論点は README §10 に一覧化しました。** 先行実装はこれらで止めないでください。主なものは、失敗行CSVの書式（外部勤怠は列マッピングを経るため「元テンプレート」が存在しない）、取込元CSVにコード列がある場合の衝突予防、複数店舗のマッピングです。

モックの外部勤怠画面（`s-saas*` 系）には**「後続フェーズ・参照用」のバナー**を表示しています。

---

# Q1. ①〜③設定マスタに「すべて」「エラー」タブは不要か

> 「①〜③設定マスタ」の業態・店舗・雇用区分のタブの下に「すべて」と「エラー」は不要でしょうか。（④従業員、⑤管理者では表示されている）モックでは表示のみですが、フィルタすべきかについてもご確認いただきたいです。

## 回答：不要です。①〜③と④⑤ではタブの軸が違います

| 対象 | タブの軸 | 内容 |
|---|---|---|
| ①〜③ 設定マスタ | **データ種別** | `業態 (2)` / `店舗 (2)` / `雇用区分 (3)` |
| ④ 従業員 | **行ステータス** | `すべて (8)` / `エラー (1)` |
| ⑤ 管理者 | **行ステータス** | `すべて (6)` / `差し替え (1)` / `スキップ (1)` / `エラー (1)` |

①〜③は**1画面に3つのマスタが同居する**ためタブ行を種別切替に使い切っています。ここにステータス軸を足すと1本のタブ行に2軸が混在し、「業態」と「エラー」が横に並ぶ形になって何の切替か分からなくなります。

## ①〜③のエラーはタブではなくこう表示します

単票プレビューの雇用区分が実装例です。

1. 上部に赤いバナー（例: `1件のエラー: 給与体系の値が不正 (許可値以外)`）
2. 該当行を淡赤背景でハイライト
3. 行内に3択アクション（`➕ マスタに追加` / `✏️ 行編集` / `⊘ スキップ`）
4. 件数はサマリーボックスのエラー枠に表示（①〜③にも存在します）

## フィルタの要否：④⑤は実装では必ずフィルタさせてください

**モックが表示のみなのは静的HTMLの制約であって仕様ではありません。** エラータブの存在価値は問題行に即座に飛べることなので、押せないタブは機能を失うだけでなく、押せると思った利用者に不信感を与えます。`すべて` / `エラー` / `差し替え` / `スキップ` の全てを実フィルタにしてください。

## 対応

| 対象 | 変更 |
|---|---|
| モック | **変更なし。** 現行の構成が正しいため |
| 仕様書 | **§7.2 に「タブの軸はデータ種別ごとに異なる」を新設。** 上記の表・①〜③のエラー表示4点・④⑤の実フィルタ化を明記。将来ステータス絞り込みが必要になっても「種別タブを主軸のまま、表の上にチップを置く。タブ行には足さない」も記載 |

---

# Q2. 雇用区分の給与形態に日給を追加するか

> 現実装では給与形態は時給・月給のみですが、モックでは日給が表示されておりました。日給を追加するという認識でよろしいでしょうか。

## 回答：追加しません。モック側の誤りでした

ご指摘のとおりです。**現行実装の時給・月給の2値が正しく、モックを修正しました。**

根拠は3点です。

**① DBが2値のenumで固定されている**
```
src/db/schema/enums.ts:8
pgEnum("employment_category", ["時給", "月給"])   ← feature/189 でも同じ
```

**② コード全体が2値前提で、日給を足すと給与計算が壊れる**

| 箇所 | 内容 |
|---|---|
| `src/app/api/employee/profile/route.ts:86,197` | `category === "時給" ? "hourly" : "monthly"` |
| `src/utils/salary-calculations.ts:148` | `isMonthly ? "月給" : "時給"` |
| `src/utils/salary-calculations.ts:184`、`src/hooks/use-salary-report.ts:13,33` | 型が `"時給" \| "月給"` |

特に `profile/route.ts` は **else 分岐で全て monthly に落ちる**ため、日給を追加してもエラーにならず**サイレントに月給として計算されます**。

**③ 連携先の経営管理にも日給の受け皿がない**

マッピング先は `給与.日給月給制` と `給与.時給制` の2区分だけです。**「日給月給制」は欠勤時に日割り控除する月給制**であって、日給ではありません。

**補足：テンプレートの日給サンプル自体も壊れていました**
```
日給スタッフC  EMP-TYPE-003  日給  10000  8
                                        └ 月次契約労働時間の列に1日の所定労働時間8hを誤って配置
```
仕様が詰まらないまま列だけ埋まっていた形跡です。

## 対応

| ファイル | 変更前 → 変更後 |
|---|---|
| `README.md` §1.2 ③ | `月給 / 日給 / 時給` → **`月給 / 時給`** |
| `入力ガイドv2.tsv` | 同上 |
| `設定データインポートマッピング表.tsv` | `月給/日給/時給` → **`月給/時給`** |
| `③雇用区分.tsv` | `日給スタッフC / 日給 / 10000 / 8` → **`アルバイトC / 時給 / 1100 / 空欄`** |
| `④従業員.tsv` | 高橋美咲の雇用区分名を **`アルバイトC`** に |
| `index.html` | 給与体系の `options` 配列、エラー行の許可値表示、プレビュー行7箇所、テンプレDL生成関数 |
| `README.md` §8.10 | 削除の判断と根拠を記録 |

> **経営管理側のマッピング先（`給与.日給月給制` / `給与.日給制`）は変更していません。** 経営管理のフィールド名であり、タイムレコードの給与体系とは別物です。

日給払いの顧客が現れた場合は、enum のマイグレーション・給与計算・経営管理側の受け皿設計を含む**別案件として起票**します。本機能のスコープには入れません。

---

# Q3. DBへのインポート失敗時の処理

> インポート実行時にDBへの取り込みでエラーになった場合の動作がわからずでした。CSV読み込み → 別プロセスにて重複するようなデータを手動で登録 → インポート実行、という操作の場合などにエラーが発生しそうだったのですが、その場合は「CSVのインポートに失敗しました」というような汎用メッセージを表示してリトライを促すような流れでよろしいでしょうか？

## 回答：汎用メッセージは使いません。行単位の成否開示が必須です

**理由：バッチ全体のトランザクションが張れないためです。** `neon-http` ドライバは `db.transaction()` に非対応で、途中で失敗しても巻き戻せません。

この状態で「インポートに失敗しました。もう一度お試しください」だけを出すと、**成功分がDBに入ったまま**利用者に伝わらず、全件やり直しを促すことになります。二重登録の温床です。**ロールバックできないからこそ、行単位の開示が必要**になります。

## ご指摘のシナリオへの具体的な回答

**1. その行だけを失敗扱いにし、全体失敗にしない**

失敗理由は汎用文言ではなく専用の文言にしてください。

> `既に登録済みのため取り込めませんでした（他の操作で登録された可能性があります）`

ユニークキーは以下です。**突合は必ず `companyId` で絞ってください**（理由は後述の §8.1）。

| エンティティ | ユニークキー |
|---|---|
| 従業員 | 従業員コード |
| 雇用区分 | 雇用区分コード（空欄時は雇用区分名） |
| 店舗 | 店舗名 |
| 業態 | 業態名 |

**2. 全件やり直しではなく「再取り込み」に誘導する**

再取り込み時は既存レコードが見えるため、その行は「重複時の処理」設定に従って自動判定され、二重登録になりません。管理者は5分岐判定でスキップに落ちます。

**3. 汎用メッセージを使うのは、行単位に切り分けられない障害だけ**

DB接続断・タイムアウト・5xx など。成功件数が0であることを明示してください。

> `取り込みに失敗しました。データは1件も登録されていません。時間をおいてもう一度お試しください。`

## ロールバックの粒度：行単位の補償削除

バッチ全体は巻き戻さず、**失敗した行だけをDBに残しません**。

さらに調査したところ、**④従業員は1行の取り込みが最大3ステップに分かれます**（後述の §8.2）。「行単位」とは**この3ステップ全体**を指します。

| ステップ | 処理 | 失敗時に補償削除する対象 |
|---|---|---|
| 1 | `POST /api/employees`（＋Clerk招待） | なし |
| 2 | `PUT /api/employees/[id]/commuting-allowance` | ステップ1のユーザー＋店舗割当＋**Clerk招待の revoke** |
| 3 | `PUT /api/employees/[id]/stores`（複数店舗時） | ステップ1・2のすべて |

- **交通費だけ未設定のユーザーを残すことは認めません。** 交通費は必須★のため、未設定のまま残すと勤怠計算の前提が崩れ、画面上も不備として見えません
- **Clerk側の招待も取り消してください。** ユーザーレコードだけ消して招待が残ると、招待メールのリンク先にアカウントが無い状態になります
- 補償削除自体が失敗した行は「**要手動確認**」として完了画面に別枠表示し、再取り込み対象から除外します（再取り込みすると重複するため）

## 対応

| 対象 | 変更 |
|---|---|
| 仕様書 | **§7.7 を新設**（ロールバック粒度・エラー3分類・文言）。その後 §8.2 の発見を受けて**多段呼び出し前提で全面改訂** |
| モック | 完了画面4箇所に**部分成功の状態**（`10件成功 / 1件失敗` ＋ 失敗行テーブル ＋ 再取り込み案内）を実装 |
| モック | **「失敗した行をCSVでダウンロード」を追加**（後述） |
| モック | `demoResult()` のコメントが確定仕様と矛盾していたため修正 |

---

# 回答の過程で見つかった追加の課題

Q1〜Q3に答えるためテンプレート列定義をアプリ側実装と突き合わせたところ、**未確定または実装と矛盾する項目が10件**見つかりました。全件決定済みです（§8）。

## 特に重要な2件

### §8.1 従業員コード・雇用区分コードのUNIQUEが全社横断

```sql
CONSTRAINT "employment_types_employee_code_unique" UNIQUE("employee_code")
CONSTRAINT "users_employee_code_unique" UNIQUE("employee_code")
```

`company_id` との複合ではなく**テーブル全体でユニーク**です。製品側でも既知課題として認識されています（`MULTI_COMPANY_IMPLEMENTATION_STATUS.md` の Known Issues）。

| 影響 | 対応 |
|---|---|
| **配布テンプレのサンプル値が2社目以降で必ず衝突**（`EMP-001` 等が実データ行に入っていた） | **サンプル行のコードを空欄化**。備考も「空欄推奨（自動生成）」に変更 |
| §4.3 のユニークキーに突合スコープの記載がなく、**全社横断で突合すると他社レコードを上書き**する | §4.3 に「**突合は必ず `companyId` で絞る**」を明記 |
| 「空欄時は自動生成」の採番規則が未定義 | `EMP-{companyId先頭8文字}-{連番4桁}` / `ETP-{companyId先頭8文字}-{連番4桁}` を定義 |

### §8.2 従業員1件の取り込みに最大3リクエスト

`createEmployeeSchema` が受け取るのは7項目のみで、**交通費のフィールドがありません**。専用エンドポイントが別にあります。

```
clerkUserId / employeeCode / name / email / phone / employmentTypeId / storeId
```

§7.5 の上限根拠は「1行につきClerk招待API 1回」でしたが、**実際は1行あたり2〜3リクエスト**です。

**→ ④従業員のみ上限を200行に変更しました。**

| 画面 | 上限 | 受付拒否の文言 |
|---|---|---|
| ④従業員の単体取込・補完テンプレート | **200行** | `1回に取り込めるのは200行までです。…（アップロードされたファイル: 480行）` |
| ①②③⑤の単体取込 | 500行 | `1回に取り込めるのは500行までです。…（アップロードされたファイル: 1,240行）` |
| 一括取込（複数種別） | 500行 | 上記＋`※ ④従業員は200行までです。` |

画面の取込対象種別に応じて文言を出し分けます（`activeEntityKey()`）。種別が確定している画面で「※ ④従業員は200行」と注釈を足すことはしません。

## その他8件（すべて決定済み）

| § | 項目 | 決定 |
|---|---|---|
| 8.3 | 複数店舗勤務 | ④は単一店舗のまま。複数は取込後に画面から追加。④⑤の非対称は意図的（⑤の担当店舗＝権限範囲、④の店舗名＝主たる勤務先） |
| 8.4 | 月みなし残業時間 | `employment_types.shift_restriction`（**text型**）にマッピング。0〜999の整数検証を取り込み側で担保 |
| 8.5 | 業態名・店舗名の突合 | 会社内・名称の完全一致（前後空白のみトリム）。DB側に同名が複数ある行はエラー |
| 8.6 | 雇用区分キーの or | コードがあればコード優先、空欄なら名前。名前で複数一致はエラー |
| 8.7 | CSVの空欄セル | **「変更しない」。NULL上書きはしない。** 必須★列が空欄の行はエラー |
| 8.8 | 列バリデーション | 実効上限はAPIスキーマ側に合わせる（**従業員名は255ではなく100**）。金額は全角・カンマ・円記号を正規化。電話番号は必須★に寄せる |
| 8.9 | 退職者との突合 | `retired` / `inactive` と同コードの行は**エラー**。自動復活させない。`inviting` は差し替え |
| 8.10 | 給与体系の日給 | 削除（Q2） |

---

# モックに追加した状態遷移（§9・7件）

モックは正常系と行レベルのエラーを網羅していましたが、以下の状態が存在しませんでした。**すべて定義しモックに実装済み**です。

| § | 状態 | モックの実装箇所 | デモ操作 |
|---|---|---|---|
| 9.1 | 取り込み実行中 | `#import-running-overlay` / `runImport()` / `_importRunning` / `beforeunload` | インポート実行ボタンを押す |
| 9.2 | 失敗行CSVダウンロード | `downloadFailedRowsCsv()`（完了画面4箇所） | 完了画面のデモトグル「一部失敗(429)」 |
| 9.3 | 上限超過の受付拒否 | `demoUploadReject()` / `.upload-reject-box`（全画面へ動的注入） | アップロード画面のデモ「行数超過」「サイズ超過」 |
| 9.4 | 全件エラー（0件） | `demoZeroTarget()` / `.zero-target-box` / `[data-import-exec]` | プレビュー画面のデモ「全件エラー(0件)」 |
| 9.5 | ファイル形式エラー | `UPLOAD_REJECT_MSG` | アップロード画面のデモ「文字コード」「列不一致」「0件」 |
| 9.6 | 権限による出し分け | `setHubRole()` | インポートハブのデモ「オーナー」「管理者(店長)」 |
| 9.7 | 除外行の完了画面での開示 | `renderExcludedRowsBlocks()`（完了画面4箇所） | 完了画面を開くと常時表示 |

受付拒否バナーと0件状態は**アクティブ画面へ動的注入する共通実装**のため、静的画面・ウィザード・動的生成される単体取込画面のいずれでも同じ挙動になります。

## 特に重要な2件

**§9.1 実行中の状態** — 進捗もスピナーもなく、**実行ボタンの二重押下防止もありませんでした**（既存の `disabled` は「必須項目未達で次へ進めない」用途のみ）。連打で多重実行される状態でした。

> 実装時はモックの擬似進捗ではなく、**サーバーから返る実際の処理済み件数**で更新してください。§8.2 のとおり1行あたり最大3リクエストのため、リクエスト単位ではなく**行単位**でカウントします。

**§9.2 失敗行CSVダウンロード** — 「失敗した1件を再取り込み →」がアップロード画面の先頭に戻すだけで、**失敗行だけのCSVを作る手段がありませんでした**。50行失敗したら画面の表を見ながら元CSVから手で拾うことになります。Q3の「再取り込みで回収できます」は、この動線なしでは成立しません。

- 書式は**元テンプレートと同一列＋末尾に「失敗理由」列**。そのまま修正して再アップロードできます
- ファイル名は `{元テンプレート名}_失敗行_{YYYYMMDD}.tsv`
- **プレビューで除外した行も含まれます**（§9.7）

---

# エラーは発生タイミングで3種類あります

どこに表示されるかが異なります。**Q3で扱ったのは③のみ**でした。

| タイミング | 種類 | 表示場所 | 完了画面 |
|---|---|---|---|
| **① アップロード時** | 行数超過・サイズ超過・文字コード・列名不一致・データ0件 | アップロード画面の受付拒否バナー | **出ない**（ファイルが受理されておらず、取り込みセッションが始まっていないため） |
| **② プレビュー時** | 値不正・マスタ未登録・退職者・担当店舗外 | プレビューの行ハイライト＋エラー列 | **出る**（§9.7で追加） |
| **③ 実行時** | 招待API 429・キー衝突・補償削除失敗 | 完了画面の失敗行テーブル | 出る |

②は従来、完了画面から消えていました。15行中1行がエラーだと「14件すべて成功」とだけ表示され、**利用者が除外分を放置します**。③には手厚い回収動線があるのに②には何もない状態だったため、完了画面に「**取り込まなかった行 (N件) — プレビューでエラーのため除外**」を常時表示するようにしました。**失敗行CSVにも②③の両方が含まれます。**

---

# 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `README.md` | **§7.2 タブの軸**（Q1）／§4.3 companyIdスコープ・空欄セルの意味／§7.5 ④のみ200行／**§7.7 失敗ハンドリング**（Q3）／**§8 新設・10件**／**§9 新設・7件**／§1.2 列定義（給与体系2値・コード空欄推奨・店舗単一） |
| `index.html` | 日給の削除（Q2）／サンプルコードの空欄化／§9の7状態を全画面に実装／取込上限の種別別出し分け／完了画面の「次にやること」／空欄セル注記／退職者・担当店舗外のエラー行 |
| `③雇用区分.tsv` | 日給行を時給行に、コード空欄化 |
| `④従業員.tsv` | 雇用区分名を `アルバイトC` に、コード空欄化 |
| `入力ガイドv2.tsv` / `マッピング表.tsv` | 給与体系の許可値を2値に |
| `audit_spec_mock.py` | **新規**。仕様書とモックの整合を機械的に検査 |

---

# アプリ側（`rakmy/rakmytimerecord`）へのお願い

**いずれも本機能のリリースをブロックしません。** 現行API構成のままで実装できる仕様にしてあります。

| 優先度 | 内容 | 効果 |
|---|---|---|
| **ブロッカー** | `feature/189` を `main` へマージ | `stores.business_hours` が `main` に無く、テンプレート②の営業時間列が成立しない |
| 中 | `createEmployeeSchema` を交通費まで受け取れるよう拡張（§8.2） | ④のリクエスト数が1/2〜1/3に。補償削除の分岐がほぼ消え、上限を500行に戻せる |
| 中 | `business_types.label` / `stores.name` に `(company_id, name)` の複合ユニーク追加（§8.5） | 同名重複を入口で防げる。現状はインポート側で後始末している |
| 中 | `(company_id, employee_code)` の複合ユニークへ移行（§8.1） | 採番規則から会社識別子プレフィックスを外せる |
| 低 | `shift_restriction` → `deemed_overtime_hours`（integer）へリネーム＋型変更（§8.4） | カラム名から用途が読めず、将来の担当者が必ず誤解する |

---

# モックの確認先

https://suguru789987.github.io/rakmy-timerecord-import-spec/

各画面のオレンジ色の破線枠（🛠 デモ）から、追加した状態を確認できます。

**最短ルート**
1. **トップ** → 「管理者(店長)」（§9.6）
2. **テンプレ① アップロード** → 「行数超過」「文字コード」「列不一致」（§9.3・§9.5）
3. **テンプレ② 確認・実行** → ④従業員テーブル10・11行目のエラー行 → 「全件エラー(0件)」（§9.4）→ 「通常」に戻して実行（§9.1）
4. **テンプレ③ 完了** → 「一部失敗(429)」でCSVダウンロード（§9.2）→ 「取り込まなかった行」（§9.7）
5. **単体取込 → 従業員** → 上限が200行になっていること（§8.2）
