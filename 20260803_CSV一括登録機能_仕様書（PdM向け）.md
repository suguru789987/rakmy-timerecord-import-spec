# CSV一括登録機能 仕様書（PdM向け）

**作成日**: 2026-08-03
**対象**: ラクミー タイムレコード「CSVで一括登録する」機能

> **後任のPdM、または仕様判断を引き受ける人が読む資料**です。
>
> **本書がインポート機能の仕様の正本（読み物としての）です。** 開発者向けの詳細な決定事項は `README.md`（§0〜§11）にあります。
>
> 本書は「どういう操作の流れで、何のために、どう動くか」に加え、**なぜその仕様になったのか、どこが未確定か、どこに触れると壊れるか**を扱います。
>
> ⚠️ **PdM（@suguru789987）の最終出社は 2026-08-19。以降この仕様を維持する担当は不在です。**

---

# 概要

## 全体のフロー

**どの入口から入っても、この4段階を通ります。**

```
  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
  │アップロード│ → │  確認   │ → │  実行   │ → │  完了   │
  └─────────┘   └─────────┘   └─────────┘   └─────────┘
   テンプレDL      判定と誤りを    進捗を表示     結果と
   入力・投入      ここで提示      二重押下防止   招待メール

   ↑ 不正なら停止  ↑ CSVで直して戻る            ↑ CSVで直して戻る
```

**確認画面が要です。** 取り込む前に誤りを提示し、CSVを直して戻らせる。この往復で誤ったデータを登録させません。

**設計上の中心的な判断は「画面で直させない」こと**です。画面で直すと元CSVに残らず再発し、プレビューが編集画面になって状態管理が跳ね上がります（J-029）。

## 入口と取り込み範囲

| 入口 | 使う場面 | 範囲 | 終了後の戻り先 |
|---|---|---|---|
| オンボーディング「CSVで一括インポート」 | 導入時に1回 | ①〜⑤まとめて | 初期セットアップ |
| 左ナビ「インポート」 | 運用中・随時 | 選んだ種別だけ | インポートTOP |
| 各設定画面「CSVインポート」 | その設定を直したいとき | その種別だけ | **元の設定画面** |

**戻り先を入口で変えている**のは、設定画面から来た人を元の作業に戻すためです（`_singleEntryFrom` で判定）。

## データの依存関係

```
① 業態 ──→ ② 店舗 ──┐
                      ├─→ ④ 従業員
           ③ 雇用区分 ─┘
                      └─→ ⑤ 管理者
```

**取り込み順序 ①→②→③→④→⑤ はこの依存から来ています。** 名称で引き当てるため、参照先が未登録だとエラーになります。

---

# ケース別の遷移フロー

## 正常系

```
アップロード → 確認 → 実行 → 完了
```

## 異常系・分岐

| ケース | 遷移 | 設計の要点 |
|---|---|---|
| **ファイルが不正** | アップロード（**停止**）→ 直す → アップロード | 行数超過・文字コード・列名不一致・0件。**ファイル全体の問題なので行単位で直せない。** プレビュー生成前に弾く |
| **一部の行に誤り** | 確認（**エラー行CSVを落とす**）→ 直す → アップロード | 元CSVを正とする。理由列を付けて返し、直したら理由列を削除して再投入 |
| **誤った行を捨てて進む** | 確認 → **エラー行を除いて実行** → 完了 | 30人中1人の未確定で全体を止めない。**除外分は完了画面に残して忘れさせない** |
| **実行中に一部が失敗** | 実行 → 完了（**失敗行CSV**）→ 直す → アップロード | 成功行は残す。**失敗行の痕跡はDBに残さない。** 再投入しても既存行はスキップ判定で二重登録にならない |
| **途中で作業をやめる** | アップロード（**一時保存して中断**）→ 後日「続きから再開」 | 従業員の連絡先・交通費は当日に揃わない。**1件も投入していない状態では保存させない** |
| **既存データを直す** | 確認（**上書き更新**を選ぶ）→ 実行 | ④従業員のみ。**空欄の列は変更しない**（消すと元に戻せない） |
| **管理者に昇格させる** | 確認（**5分岐で自動判定**）→ 実行 | メール突合。**既存の勤務店舗を必ず維持する**（消えると打刻できなくなる） |
| **店長が取り込む** | 権限で種別が絞られる | 全社マスタ（①②③）は非表示。④⑤は担当店舗の範囲のみ |

> **上記のうち「既存データを直す」「管理者に昇格させる」は運用更新でのみ発生します。** 初期セットアップでは既存データが無いため、判定はほぼすべて「新規登録」になります。詳細は「新規登録と運用更新の違い」を参照。

**太字が、それぞれのケースで守るべき設計判断です。** 詳細と理由は「4. 主要な仕様判断とその理由」にあります。


---

# 新規登録と運用更新の違い

**同じ4段階を通りますが、モードによって前提が変わります。** この違いを理解しないと、検証で「なぜここは新規しか出ないのか」と迷います。

| | 新規登録（初期セットアップ） | 運用更新（既登録あり） |
|---|---|---|
| **いつ** | 導入時に1回 | 運用開始後、随時 |
| **入口** | オンボーディング「CSVで一括インポート」 | 左ナビ「インポート」／各設定画面「CSVインポート」 |
| **取り込む範囲** | ①〜⑤をまとめて（順番に沿って） | 選んだ種別だけ |
| **既存データ** | **無い**（0件から始まる） | **ある**（既存と突き合わせる） |
| **判定に出るもの** | ほぼ**新規登録**のみ | **新規／更新／スキップ／差し替え**が混在 |
| **「重複時の処理」** | 事実上効かない（重複する既存が無い） | **重要な設定**。既存を守るか上書きするかを決める |
| **主なエラー** | 参照先が未登録（順番違い） | 重複・退職者と同じコード・担当店舗外 |
| **招待メール** | 登録した全員に送る | 追加・昇格した人だけに送る |
| **終了後の戻り先** | 初期セットアップ画面 | 入口による（インポートTOP／元の設定画面） |
| **中断・再開** | **必要**（5種類は1日で揃わない） | ほぼ不要（1種類で完結する） |

## なぜ入口を分けているか

**必要な操作の量が違うためです。**

初期セットアップは5種類を順番に揃える必要があり、**依存関係を守らせるガイド**が要ります。一方、運用更新は「従業員を5人追加したい」だけなので、5種類分の画面を通らせるのは邪魔です。

同じ画面で両方をこなそうとすると、初期の人には情報が足りず、運用の人には手順が多すぎる形になります。

## 検証で注意すること

**「重複時の処理」は運用更新でしか意味を持ちません。**

初期セットアップでは既存データが無いため、スキップと上書き更新のどちらを選んでも結果は同じです。**この設定を本当に検証できるのは、一度取り込んだ後の再取り込み（フローE）だけ**です。

**5分岐の判定も同様です。**

⑤管理者の「権限付与」「差し替え」「スキップ」は、**既に従業員や管理者が登録されている状態でないと発生しません。** 初期セットアップの1回目ではすべて「新規登録」になります。

検証プランで P7（再取り込み）を後半に置いているのは、この順序が必要なためです。

---

# 登録・編集の全体像（手入力とインポート）

**利用者がデータを登録・編集する経路は3つあります。** 本書はそのうち「CSVインポート」を扱いますが、**3経路は同一のデータ契約の上にあります**。この関係を理解しないと、インポート単体では成立しない仕様（後述）を見落とします。

## 3つの経路

| 経路 | 何をするか | 仕様の所在 |
|---|---|---|
| **登録モーダル**（手入力・新規） | 1件ずつ登録。「+ 追加」から開く | owner-mock §3.4 |
| **編集モーダル**（手入力・編集） | 一覧の各行「編集」から開く。同一モーダルをプリフィルで再利用 | owner-mock §3.4.2 |
| **CSVインポート** | まとめて登録・更新 | **本書 / README** |

## 3経路は同一スキーマ（重要）

owner-mock §3.4.1 で次のルールが確定しています。

> エンティティの属性スキーマは **CSVインポートテンプレを正（データ契約）** とし、登録モーダル・編集モーダル・import の3経路で**同一項目を捕捉**すること（経路による属性欠落を作らない）。任意項目（コード類）は「空欄で自動生成」で揃える。

**つまり CSVテンプレートの列定義が、手入力フォームの項目も規定しています。**

| エンティティ | 統一スキーマ（登録＝編集＝CSVテンプレ） |
|---|---|
| ① 業態 | 業態名 |
| ② 店舗 | 店舗名／所属業態／郵便番号／住所／営業時間 |
| ③ 雇用区分 | 雇用区分名／雇用区分コード／給与体系／金額／月次契約労働時間／月みなし残業時間 |
| ④ 従業員 | 名前／電話／メール／従業員コード／雇用区分／所属店舗／交通費単位／交通費 |

> ⚠️ **本書で列定義を変更する場合、手入力の登録・編集モーダルにも同じ変更が必要です。** 片方だけ変えると、経路による属性欠落が発生します。owner-mock 側の担当者へ連携してください。

## 操作ごとの対応

| 操作 | 手入力 | CSVインポート |
|---|---|---|
| 新規登録 | ○ 1件ずつ | ○ まとめて |
| 更新 | ○ 各行「編集」 | ○ ④従業員のみ（「上書き更新」を選択時） |
| **削除** | ○ 各行「削除」 | **✕ できない** |
| 値を空にする | ○ 編集モーダルで消す | **✕ できない**（空欄は「変更しない」） |
| ⑤管理者の担当店舗の変更 | ○ 管理者一覧から | **✕ できない**（既存管理者はスキップ判定） |
| 複数店舗の割り当て | ○ 従業員一覧から追加 | **✕ できない**（④は1店舗のみ） |
| 退職者の復帰 | ○ 従業員一覧から | **✕ できない**（エラーになる） |

## インポート単体では成立しない仕様

**上の表の「✕」は、インポートの仕様が手入力を前提にしている箇所です。**

| インポートの仕様 | 前提としている手入力の操作 |
|---|---|
| ④の店舗は1店舗のみ | 複数店舗は**取込後に従業員一覧から追加** |
| 空欄の列は変更しない | 値を消したい場合は**設定画面から編集** |
| 既存管理者はスキップ | 担当店舗の変更は**管理者一覧から** |
| 退職者と同じコードはエラー | 再雇用は**従業員一覧から復帰の手続き** |
| **削除する手段がない** | 誤って取り込んだデータは**一覧から1件ずつ削除** |

**これらの出口が塞がっていると、インポートの仕様が成立しません。** 受け入れ条件 AC-057（出口が塞がっていない）と AC-059／AC-060（編集・削除ができる）で判定します。

### 削除について

**インポートには削除機能がありません。** CSVから行を消しても、既に登録されたデータは残ります。

これは意図した仕様です。CSVの記載漏れが大量削除を引き起こすことを防ぐためです。ただし次の2点で手入力の削除が必要になります。

**誤って取り込んだ場合の唯一の復旧手段です。** 巻き戻し機能はないため、一覧から1件ずつ削除します。

**検証の再実行にも必要です。** テストデータを消せないと、同じCSVで2回目の確認ができません。検証プランでは OP-615〜617 でこの一巡を確認します。

## 新規登録モードと運用更新モードは手入力にも存在する

owner-mock §3.3 で、手入力側も同じ2モードに分かれています。

| | 手入力 | CSVインポート |
|---|---|---|
| 新規登録モード | `*-settings`（初期セットアップから。中央CTAモーダルで次へ誘導） | オンボーディングの「CSVで一括インポート」 |
| 運用更新モード | `*-ops-settings`（サイドバー「設定(運用)」。右上トースト） | 左ナビ「インポート」／各設定画面 |

**モードの考え方は両経路で共通です。** 詳細な対比は次章「新規登録と運用更新の違い」を参照してください。

---

# 1. この機能の位置づけ

## 1.1 何を解決しているか

**導入時の初期登録が、1件ずつでは終わらない。**

タイムレコードは打刻のためのプロダクトですが、打刻を始めるまでに業態・店舗・雇用区分・従業員・管理者の登録が必要です。従業員30人・3店舗で数時間かかります。**導入の離脱がここで起きます。**

CSV一括登録は、この時間を数十分に縮めるための機能です。**打刻機能そのものの価値ではなく、そこに到達するまでの障壁を下げる**位置づけです。

## 1.2 スコープの変遷

| 時期 | 状態 |
|---|---|
| 2026-03-31 | 初版。テンプレート2種（設定マスタ・従業員データ） |
| 2026-06-24 | 5テンプレート構成に分割（①〜⑤） |
| 2026-07-16 | 初期セットアップ（一括）と運用更新（単体）に入口を分離 |
| 2026-07-23 | 実装との照合を開始（§7） |
| 2026-07-28 | 実装照合で10件、状態遷移で7件を確定（§8・§9） |
| **2026-07-31** | **外部勤怠サービスインポートを凍結。実装対象をテンプレートのみに確定** |

**現在の実装対象はテンプレートインポートのみです。**

---

# 2. 判断の土台（触ると壊れるもの）

## 2.1 前提条件は §0 にある

**`README.md` §0 前提条件が、この仕様のすべての土台です。** 仕様を変更する前に必ず読んでください。

| | 内容 |
|---|---|
| §0.1 | 照合基準ブランチは `origin/feature/189`（`main` に未マージ） |
| §0.2 | 実装対象はテンプレートのみ。外部勤怠は凍結 |
| §0.3 | **ロール区分は管理者（全権・店長）と従業員の2つのみ** |
| §0.4 | 勤務店舗と担当店舗は単一テーブル |
| §0.5 | 給与体系は時給・月給の2値 |
| §0.6 | コードは全社横断でユニーク／自動採番は既存実装あり |
| §0.7 | **守るべき制約はDB側のみ。それ以外は実装の選択** |
| §0.8 | バッチのトランザクションが張れない |

**§0 に書かれていない前提で判断してはいけません。** 「一般的なSaaSではこうだ」という類推が、このプロジェクトで2回事故を起こしています（後述の 5.2）。

## 2.2 特に誤解しやすい3点

**(1) ロールは3値で、カスタム権限は作れない**

`roleEnum = ["owner", "manager", "employee"]`。エリアマネージャーのような「管理するが勤務しない」役割は**存在しません**。

ラクミー**経営管理**の権限設定（`master` / `manager` / `shop_manager` ＋カスタム権限、「権限は権限、範囲は割当」）とは**別プロダクトの別モデル**です。混同すると設計判断を誤ります。

**(2) 勤務店舗と担当店舗は同じテーブル**

`userStoreAssignments` の1つだけです。**店長の担当店舗＝勤務店舗**なので、この設計は本製品の役割設計に対して正しいです。分離は不採用としました（J-028）。

**(3) 実装の選択と仕様を混ぜない**

「1行あたり3リクエスト」「実効上限100文字」「担当店舗が置換される」は、**手入力の登録処理を再利用した場合の性質**であって仕様ではありません。DBへ直接書く実装も取り得ます（§11.1 が未決）。

**どう実装しても回避できないのはDB側の制約だけ**です。

---

# 3. 主要な操作フロー（詳細）

## フローA　初期セットアップ（新規登録モード・全種別を一括）

### 目的

**導入時に、必要なマスタと従業員をまとめて登録し、業務を開始できる状態にする。**

### 流れ

```
オンボーディング画面
  └ 「CSVで一括インポート」を選ぶ
       ↓
【1】アップロード画面
  ・①業態 → ②店舗 → ③雇用区分 → ④従業員 → ⑤管理者 の順に
    テンプレートDL → 入力 → アップロード を繰り返す
  ・必須4種（①〜④）が揃うと「確認画面へ」が押せるようになる
       ↓
【2】確認画面（プレビュー）
  ・種別タブで ①〜③設定マスタ／④従業員／⑤管理者 を切り替えて確認
  ・誤りがある行はエラーとして表示される
  ・「重複時の処理」（スキップ／上書き更新）を選ぶ
       ↓
【3】取り込み実行
  ・進捗が表示され、完了すると自動で完了画面へ
       ↓
【4】完了画面
  ・登録結果（成功／失敗の件数）
  ・取り込まなかった行の一覧
  ・招待メールの一括送信
  ・「次にやること」の案内
```

### 各段階の目的

**【1】アップロード画面 — 順番を守らせ、抜けを防ぐ**

①→⑤には依存関係があります。店舗は業態を参照し、従業員は店舗と雇用区分を参照します。**順番を間違えると引き当てに失敗する**ので、画面が順に案内します。

必須4種が揃うまで先へ進めないのは、**中途半端な状態で取り込ませない**ためです。従業員だけ登録しても、店舗と雇用区分が無ければ打刻できません。

**【2】確認画面 — 取り込む前に誤りに気づかせる**

この機能で最も重要な画面です。**取り込んでから間違いに気づくと、消して入れ直す作業が発生します。**

表示するもの。

- **件数のサマリ** — データ行数／新規／更新／スキップ／エラー
- **1行ごとの判定** — その行がどう処理されるか
- **エラーの理由** — なぜ取り込めないか

「重複時の処理」をここで選ぶのは、**既存データを上書きするかどうかは取り込み直前に判断すべき**ことだからです。

**【3】取り込み実行 — 処理中であることを示し、二重実行を防ぐ**

従業員200件では時間がかかります。無表示だと利用者が「固まった」と判断してボタンを連打し、**二重登録が起きます**。進捗表示とボタンの不活性化はこれを防ぐためのものです。

**【4】完了画面 — 結果を確定させ、次の行動につなげる**

登録して終わりではありません。**招待メールを送らないと従業員はログインできません。** 完了画面から一括送信します。

「取り込まなかった行」を表示するのは、**除外した行を忘れさせない**ためです。「14件成功」だけ見せると、除外した1件が放置されます。

---

## フローB　運用更新（既登録あり・種別ごと）

### 目的

**運用中に発生する追加・変更を、必要な種別だけ最小の操作で反映する。**

### 流れ

```
入口は2つ

（1）左ナビ「インポート」
       ↓
     インポートのトップ画面
       ・現在の取り込み方式が表示される（テンプレート）
       ・種別カード5つ（業態／店舗／雇用区分／従業員／管理者）
       ・取り込みたい種別の「取り込み →」を押す
       ↓
（2）各設定画面の「CSVインポート」
     例：従業員一覧の画面から直接
       ↓
【1】単体取込のアップロード画面
  ・取り込む種別が固定表示される（選び直す必要がない）
  ・テンプレートDL → 入力 → アップロード
       ↓
【2】確認画面（プレビュー）
  ・フローAと同じ内容。ただし取り込む種別のみ
       ↓
【3】取り込み実行
       ↓
【4】完了画面
  ・入口に応じて戻り先が変わる
      ハブから来た   → 「インポートTOPに戻る」
      設定画面から来た → 「設定画面に戻る」
```

### 各段階の目的

**入口が2つある理由 — 利用者の起点に合わせる**

「インポートしたい」と思って始める人は左ナビから入ります。「従業員を追加したい」と思って従業員一覧を開いた人は、そこから直接インポートできる方が自然です。**どちらの入り方でも同じ機能に到達できる**ようにしています。

**戻り先が変わる理由 — 元いた場所に返す**

設定画面から入った人をインポートTOPに返すと、元の作業に戻るのに操作が増えます。**入ってきた経路を覚えておき、そこへ返します。**

**取り込み方式を毎回選ばせない理由**

方式（テンプレート／外部勤怠）は初期設定で1回決めるものです。運用のたびに選ばせると、毎回同じ操作を繰り返させることになります。変更したいときだけ、トップ画面の「インポート方式を再設定」から選び直します。

> 現在、**外部勤怠サービスは凍結中**のため、実質的にテンプレート方式のみです。

---

## フローC　誤りがあったときの修正

### 目的

**誤ったデータを取り込ませず、かつ正しい行の取り込みは止めない。**

### 誤りが見つかる3つのタイミング

```
【1】アップロード時 ── ファイルごと受け付けない
      行数超過／サイズ超過／文字コード不正／列名不一致／データ0件
        ↓ 画面に理由が表示され、プレビューに進まない
        ↓ ファイルを直して再アップロード

【2】確認画面 ────── その行だけエラー
      必須項目が空欄／未登録の業態名や雇用区分名／退職者と同じコード
        ↓ 該当行が赤く表示され、理由が出る
        ↓ 「エラー行をCSVでダウンロード」
        ↓ Excelで直す
        ↓ 再アップロード
      ※ エラー行を除いて実行することも可能（正しい行だけ取り込む）

【3】取り込み実行時 ── その行だけ失敗
      実行中に別の人が同じデータを登録したなど
        ↓ 完了画面に失敗行が表示される
        ↓ 「失敗した行をCSVでダウンロード」
        ↓ 直して再取り込み
      ※ 成功済みの行は自動でスキップされ、二重登録にならない
```

### 設計の意図

**【1】をファイルごと弾く理由**

行数超過や文字コード不正は**ファイル全体の問題**で、個別の行を直しても解決しません。プレビューを生成する前に弾くことで、無駄な待ち時間をなくします。

**【2】で画面上の編集をさせない理由**

確認画面でセルを直接編集できると一見便利ですが、問題があります。

- **直した内容が元のCSVに残らない。** 次に同じファイルを使うと、また同じ誤りが出ます
- 画面が「表示するもの」から「編集して保存するもの」に変わり、開発量が大きく増えます

**元のCSVを常に正とし、そこを直させる**方が運用として閉じます。

**【3】で全部やり直しにしない理由**

100件中1件が失敗したとき、全部を巻き戻すと99件の登録がやり直しになります。**成功した行はそのまま残し、失敗した行だけを回収させます。**

再取り込みしても二重登録にならないのは、**既に登録済みの行が自動でスキップ判定される**ためです。

---

## フローD　管理者の登録・昇格

### 目的

**従業員として在籍している人を管理者にするときに、アカウントを二重に作らない。**

### 判定の流れ

⑤管理者テンプレートの各行について、**メールアドレスで会社内を検索し、状態に応じて処理を振り分けます。**

> **初期セットアップの1回目では、ほぼすべてが「新規登録」になります。** 「権限付与」「差し替え」「スキップ」は、既に従業員や管理者が登録されている運用更新の場面で発生します。

```
そのメールアドレスの人は会社内にいるか
  │
  ├ いない ──────────────────→【新規登録】
  │                              アカウントを作り管理者権限を付与
  │                              招待メールを送る
  │
  └ いる
      │
      ├ すでに管理者 ────────→【スキップ】
      │                          何もしない
      │
      └ まだ管理者ではない
          │
          ├ 招待を承諾済み ───→【権限付与】
          │                      既存アカウントに管理者権限を追加
          │                      （新しくアカウントは作らない）
          │
          └ 招待が未承諾 ─────→【差し替え】
                                 旧招待を無効化して招待し直す
```

### なぜこの分岐が必要か

**店長は従業員でもあり管理者でもあります。** 打刻もするし、勤怠の承認もします。

単純に「⑤に書かれた人を新規登録する」だけの実装だと、**④で登録した店長のアカウントとは別に、もう1つアカウントが作られます。** 同じ人が2人になり、打刻の記録も分断されます。

メールアドレスで突合して振り分けることで、**1人1アカウントを保ちます。**

### 担当店舗の扱い（重要）

**既存の従業員を管理者に昇格させるとき、その人の勤務店舗を消してはいけません。**

渋谷店と新宿店で働く従業員を、担当店舗「渋谷店」だけ書いて昇格させたとします。何も対策しないと、**新宿店の所属が消えて、その人は新宿店で打刻できなくなります。**

そのため、**CSVに書かれた担当店舗と、その人が既に持っている勤務店舗を合わせて登録します。** 利用者に選ばせる設定は設けていません。減らしたい場面が存在しないためです。

---

## フローE　再取り込み（既存データの更新）

### 目的

**同じ人・同じ店舗の情報を後から修正できるようにする。**

### 「重複時の処理」の2つの選択

確認画面で選びます。**④従業員にのみ適用されます。**

> **このフローは運用更新でのみ意味を持ちます。** 初期セットアップでは既存データが無いため、どちらを選んでも結果は同じです。

| 選択 | 動作 | 使う場面 |
|---|---|---|
| **スキップ（既存を保持）** | 同じ従業員コードの行があれば何もしない | 新しい人だけを追加したい |
| **上書き更新** | 同じ従業員コードの行を、CSVの内容で更新する | 電話番号や交通費をまとめて直したい |

### 更新される範囲

**テンプレートに書いた列はすべて更新されます。** 従業員コードだけは、どの行かを特定する鍵なので更新されません。

### 空欄にした列の扱い（重要）

**空欄の列は「変更しない」として扱います。既存の値は消えません。**

事故を防ぐためです。CSVでは「空欄」と「値を消したい」を区別できません。もし空欄を「消す」と解釈すると、**電話番号の列だけ埋めて再取り込みした人が、他の全項目を失います。** しかも元に戻せません。

値を消したい場合は、設定画面から個別に編集します。

### なぜ管理者には適用されないか

⑤管理者はフローDの5分岐で自動判定されるためです。「スキップ」も判定の1つとして既に含まれています。

---

## フローF　権限による違い

### 目的

**店長が、自分の担当範囲を超えたデータを登録できないようにする。**

### 見え方の違い

| | オーナー（全権） | 管理者（店長） |
|---|---|---|
| ①業態 | 取り込める | **表示されない** |
| ②店舗 | 取り込める | **表示されない** |
| ③雇用区分 | 取り込める | **表示されない** |
| ④従業員 | 取り込める | 取り込める（担当店舗のみ） |
| ⑤管理者 | 取り込める | 取り込める |

### なぜこう分けるか

**業態・店舗・雇用区分は会社全体の設定です。** 店長がこれを変更できると他の店舗にも影響します。担当店舗しか範囲を持たない立場の人が会社全体の設定を触れるのは、権限設計として矛盾します。

従業員の取り込みでは、**担当外の店舗名を含む行はエラー**になります。自分の店舗の従業員だけを登録できます。

---

## フローG　作業を中断して後日再開する

### 目的

**5種類のテンプレートを1日で用意できない場合に、途中まで進めた状態を保存できるようにする。**

### 流れ

```
【1】アップロード画面
  ・①〜③まで投入した時点で「一時保存して中断」を押す
       ↓
     中断地点が保存される（保存完了のモーダルが出る）
       ↓
     ── 後日 ──
       ↓
【2】オンボーディング画面
  ・「続きから再開」が表示される
       ↓
【3】アップロード画面（続きから）
  ・①〜③はアップロード済みとして復元されている
  ・④従業員から続けられる
```

### 設計の意図

**従業員の一覧はすぐに揃わないためです。** 業態と店舗はその場で決められますが、従業員の連絡先や交通費は総務に確認が必要になります。**その日のうちに全部揃えられないことが普通**なので、途中で止められる必要があります。

**1件もアップロードしていない状態では「一時保存」を押せません。** 保存するものが無い状態で押させると、「保存した」と誤認させるためです。

---

## フローH　誤りを直して取り込み直す（往復）

### 目的

**誤りを見つけてから直して完了するまでを、迷わせずに1周させる。**

### 流れ

```
【1】アップロード
       ↓
【2】確認画面 ── エラー行あり
       │
       ├─ (a) 誤りを直してから全部取り込みたい
       │       ↓
       │    「エラー行をCSVでダウンロード」
       │       ↓
       │    Excelで直す（理由列を見ながら）
       │       ↓
       │    理由列を削除して保存
       │       ↓
       │    【1】アップロードへ戻る
       │
       └─ (b) 誤った行は捨てて先に進みたい
               ↓
            「エラー行を除いてインポート実行」
               ↓
            【3】完了画面
               ・「取り込まなかった行（N件）」が表示される
               ・後から直したければ、ここでもCSVを落とせる
```

### なぜ2つの道を用意するか

**急ぎかどうかで判断が変わるためです。**

30人中1人の交通費が未確定なとき、その1人のために全体を止めるのは無駄です。**29人を先に登録して業務を始め、1人は後から追加する**方が実務に合います。

一方、店舗名の誤字のようにすぐ直せるものは、**その場で直してから全部取り込む**方が手戻りがありません。

**どちらを選んでも、除外した行は完了画面に残ります。** 忘れさせないためです。

### エラー行CSVの形

```
元のテンプレートと同じ列        ＋  エラー理由
店舗名  所属業態  郵便番号 ...      なぜ取り込めなかったか
```

**理由列は直すときに見るためのものです。** 直したら理由列を削除して再アップロードします。

---

---

# 4. 主要な仕様判断とその理由

## 4.1 設計の背骨になっている5つの判断

| 判断 | 選んだ理由 | 記録 |
|---|---|---|
| **誤りは画面で直させず、CSVで直させる** | 画面で直した内容は元CSVに残らず、次回また同じ誤りが出る。加えてプレビューが編集画面になり状態管理が跳ね上がる | J-029・§7.2 |
| **失敗した行の痕跡をDBに残さない** | バッチのトランザクションが張れないため、成功行は残し失敗行だけを消す。全件やり直しにすると99件の登録が無駄になる | J-026・§7.7 |
| **CSVの空欄は「変更しない」** | 空欄と「消したい」を区別できない。空欄＝消去にすると、1列だけ直した人が他の全項目を失い、元に戻せない | J-031・§8.7 |
| **管理者はメールアドレスで5分岐に自動判定** | 店長は従業員でもある。単純に新規登録すると同じ人が2アカウントになり打刻記録が分断される | §7.1 |
| **昇格時に勤務店舗を必ず維持する** | 消えるとその人は打刻できなくなる。画面は所属店舗を初期チェックして開くが、CSVには相当する仕組みがない | J-027・§7.1 |

## 4.2 明示的に「やらない」と決めたこと

| やらないこと | 理由 |
|---|---|
| プレビューでの行内編集（マスタ追加／行編集） | 4.1 の1つ目。工数と運用の両面で不利 |
| 「既存の所属店舗を維持する」のON/OFF選択 | オフにする場面が存在しない。⑤で登録できるのは店長のみで、担当店舗＝勤務店舗のため |
| 勤務店舗と担当店舗のテーブル分離 | 「管理するが勤務しない」役割が存在しない。影響80箇所に対し利益がない |
| ④従業員テンプレートでの複数店舗指定 | 初期取込時点では複数店舗勤務が未確定なことが多い。取込後に画面から追加する |
| 外部勤怠サービスインポート | 仕様未完成かつ担当不在。半端に残すと実装根拠にされて事故る |

**「やらない理由」を残しているのは、後任が同じ検討を繰り返さないためです。**

## 4.3 判断の全記録

`JUDGMENT_LOG.md` に **29件**の判断が記録されています（IDは J-001〜J-034 で、欠番があります）。形式は「Claude A案 → PdM B案採用 → 採用理由 → 振り返り」です。

特に **J-029（エラー行の行内アクション撤廃）は J-001 を覆した判断**です。J-001 が誤りだったのではなく、**外部勤怠側の文脈での判断をテンプレート側に無自覚に流用したこと**が問題でした。

---

# 5. 未確定・リスク

## 5.1 未決事項（後任またはエンジニアが決める）

| # | 項目 | 影響範囲 | 記録 |
|---|---|---|---|
| **1** | **インポートは手入力の登録処理を再利用するか** | §7.1 §7.5 §7.7 §8.2 §8.8 §4.4 の6箇所が変わる | §11.1 |
| 2 | ④従業員の上限行数（現在200行） | 1の判断次第。性能実測なし | §7.5 |
| 3 | 数値の上限（みなし残業999など） | 根拠が薄い暫定値 | §8.4 |
| 4 | 金額・名称の正規化ルール | 既存の正規化・突合ロジックを未確認 | §8.5 §8.8 |

**1が最優先です。** これが決まらないと仕様の6箇所が確定しません。**エンジニアの判断で確定して構いません。**

## 5.2 このプロジェクトで実際に起きた失敗

**同じ轍を踏まないために残します。**

| 失敗 | 何が起きたか | 教訓 |
|---|---|---|
| **実装を確認せず一般論で仕様を書いた** | 給与体系に「日給」を入れていた。外部サービスの語彙をそのまま許可値にしたもので、DBは2値。採番規則も既存実装があるのに独自定義していた | **許可値はDBのenumを正とする。** 外部サービスの語彙を写さない |
| **ロール設計を確認せず推測で提案した** | 「エリアマネージャーがいるはず」と推測し、テーブル分離（80箇所改修）を他部署へ依頼する文面まで作成。`roleEnum` は既に開いていたファイルの3行上にあった | **推測を根拠に他部署へ依頼しない。** 推測なら「設計判断」と明示する |
| **実装対象外の画面を実装例に引用した** | エラー行の「行内3択アクション」。先行実装に存在しない機能を要求し、エンジニアの工数見積もりが膨らんだ | **判断には適用範囲を必ず付記する** |
| **レコード単位でしか定義しなかった** | 「上書き更新」でどの列が更新されるか未定義。空欄セルの意味、担当店舗の置換も同じ形の欠落 | **列単位・データ層まで書く** |
| **仕様に書いてモックに実装しなかった** | BOM付きUTF-8、完了画面の「次にやること」。数を数えずに「4箇所に実装」と書いたこともある | **機械チェックで担保する** |

**共通する原因は「確認できるものを確認せずに埋めた」ことです。**

## 5.3 既知のリスク

| リスク | 内容 | 対応 |
|---|---|---|
| `feature/189` が `main` に未マージ | ②店舗の営業時間列が動作しない | **リリースのブロッカー**。マージを依頼済み |
| 自動採番の衝突チェックが会社内に絞られている | DB制約は全社ユニークのため、他社のコードと衝突するとすり抜けてDB制約違反 | アプリ側へ不具合として起票済み |
| 外部勤怠を顧客に案内済みの可能性 | 凍結により提供できない | 営業・CSへの確認が必要 |

---

# 6. 仕様を変更するときの手順

## 6.1 必ず整合チェックを実行する

```
python3 audit_spec_mock.py
```

終了コード0で整合、1で不整合箇所が一覧表示されます。**仕様書かモックを変更したら必ず実行してください。**

検査内容。

- README が挙げる関数名・画面ID・要素IDがモックに実在するか
- 配布先の画面数（完了画面4／プレビュー4など）
- 適用範囲ラベルの網羅と語彙の固定（4語彙以外は落ちる）
- **方式の交差汚染**（実装対象の仕様が外部勤怠の画面を実装例に引用していないか）
- §0 前提条件の存在と7項目の記載
- 仕様が「BOM付きUTF-8」と書いているなら実際にBOMが入っているか

**4番目は、5.2 の3つ目の失敗を検出するために追加したものです。**

## 6.2 変更したら記録する

| 変更内容 | 記録先 |
|---|---|
| 仕様の変更 | `README.md` ＋ `CHANGELOG.md` |
| 判断（A案を却下してB案を採った等） | `JUDGMENT_LOG.md` |
| 引き継ぎに関わること | `HANDOFF.md` |

commit message は「`判断記録: rollback判断: A（旧） → B（採用）, 理由: …`」の形式です。

## 6.3 適用範囲を必ず付ける

§7.1〜§9.7 の各節と、§1／§3／§4.x／§5 の章冒頭には**適用範囲ラベル**が必要です。語彙は4つに固定されています。

```
テンプレートのみ（先行実装）
外部勤怠のみ（後続実装）
両方式共通
方式別に定義済み
```

**語彙外を使うと監査スクリプトが落ちます。** 表記ゆれを防ぐためです。

---

# 7. 検証・引き渡し

## 7.1 用意した資料

| 資料 | 内容 |
|---|---|
| `20260803_受け入れ条件表.tsv` | リリース可否の判断基準66件。必須／推奨の別と対応する検証IDを記載 |
| `20260803_検証プラン_操作系.tsv` | 操作手順と期待値82件。10列（記入するのは実際の結果・判定の2列だけ） |
| `20260803_検証プラン_計算系.tsv` | 数値・件数・上限の確認42件。9列（記入するのは実際の値・判定の2列だけ） |
| `20260803_検証用データセット.tsv` | CSVに入力するデータ25件（D1〜D5） |
| `20260803_トレーサビリティ表.tsv` | 仕様§→受け入れ条件→検証IDの対応27節 |

## 7.2 トレーサビリティは機械的に担保している

```
仕様27節すべてがカバーされている
受け入れ条件66件すべてが実在する検証項目を参照（参照切れ0件）
検証項目124件すべてがいずれかの受け入れ条件から参照（孤児0件）
```

**検証で不合格が出たとき、どの仕様の話かを逆に辿れます。** 仕様を追加したら、この3点が保たれるよう検証項目と受け入れ条件も更新してください。

## 7.3 引き渡しの予定

| 日付 | 内容 |
|---|---|
| 8/14 | 実装状況を確認し、実装された／されなかったを仕分け |
| 8/15 | スクリーンショット取得、期待値の最終調整 |
| **8/16** | **社長へ引き渡し。以降は検証実行・Framer掲載・リリースを引き継ぐ** |
| 8/19 | PdM最終出社 |

---

# 8. 検証の範囲とリリース判断

## 8.1 検証する範囲としない範囲

検証には範囲があります。**何を確認し、何を確認しないかを明示します。** 想定外の不具合が出たときに、範囲外だったのか見落としだったのかを区別するためです。

### 検証する範囲

| 範囲 | 内容 |
|---|---|
| 機能 | テンプレートを使ったCSV一括登録（フローA〜F） |
| **データの一巡** | インポートで登録したデータを**手入力で編集・削除できるか**（OP-613〜617） |
| データ量 | 各テンプレート数件〜数十件。上限（500行・④は200行）は境界値のみ確認 |
| 権限 | オーナーと管理者（店長）の2種 |
| 環境 | Staging。ブラウザは業務で使用するもの1種 |

### 検証しない範囲

| 範囲外 | 理由 | リスク |
|---|---|---|
| **外部勤怠サービスインポート** | 仕様未完成のため凍結（第3部） | 顧客に案内済みの場合、問い合わせが発生する |
| **上限いっぱいの実データでの性能** | 500行・200行の境界値は確認するが、実際にその件数を取り込む時間は測らない | 大規模顧客で処理時間が想定を超える可能性 |
| **複数ブラウザ・複数OS** | 業務で使用する環境1種のみ | 他環境で表示崩れの可能性 |
| **同時に複数人が取り込む場合** | 単独操作のみ確認 | 同時実行時の競合は未検証 |
| **打刻・勤怠計算への影響** | 登録が正しく行われることまでを確認 | 登録後の勤怠計算は別機能の範囲 |
| **手入力機能そのものの網羅** | 既に稼働中で今回のリリース対象外。検証資材は owner-mock 側にある | ここで重複させると二重管理になる。**インポートしたデータが扱えるか（データの一巡）のみ確認する** |

**上限いっぱいの性能**は、リリース後に大規模顧客が出た時点で測ることを勧めます。上限行数（④の200行）自体が暫定値であり、実測して見直す前提です（第7部）。

### （既知のリスクは 5.3 参照）

| リスク | 内容 | 対応 |
|---|---|---|
| **`feature/189` が `main` に未マージ** | この状態では②店舗の「営業時間」列が動作しない | 検証前にマージ状況を開発側に確認する。未マージなら OP-217・OP-602 は判定できない |
| **自動採番の衝突** | 従業員コードの採番で、他社のコードと衝突するとエラーになる既知の不具合がある | 発生頻度は低いが、CA-071 で複数件採番して確認する |
| **仕様の暫定値** | 上限行数や数値の上限に確定した根拠がない | 第7部参照。運用開始後に見直す |

---

## 8.2 リリース判断の基準

### 判断の基準

| 状態 | 判断 |
|---|---|
| 「必須」がすべて合格 | **リリース可能** |
| 「必須」に不合格がある | **リリース不可**。開発側へ差し戻す |
| 「推奨」のみ不合格 | リリース可能。次回改善として記録する |

### 特に重要な2件

「必須」の中でも、**不合格のままリリースすると顧客のデータが失われて元に戻せない**ものが2つあります。

| 条件ID | 内容 | 該当フロー |
|---|---|---|
| **AC-053** | 管理者に昇格させても勤務店舗が消えない | フローD |
| **AC-063** | 空欄の列で既存の値が消えない | フローE |

他の項目は不合格でも、修正して再リリースすれば回復します。**この2つは失われたデータが戻りません。** 必ず確認してください。

---

## 8.3 フローと検証段階の対応

**検証プランの段階（P0〜P9）は、第2部のフローに対応しています。**

| 段階 | 対応するフロー |
|---|---|
| P0 前提／P1 テンプレートDL | フローA【1】の準備 |
| P2 アップロード・受付拒否 | フローA【1】／フローC【1】 |
| P3 プレビュー | フローA【2】／フローC【2】／フローD／フローH |
| P4 実行 | フローA【3】 |
| P5 完了 | フローA【4】／フローC【3】 |
| P6 データ確認 | 全フローの結果確認 |
| P7 再取り込み | フローE／フローG（中断・再開） |
| P8 権限 | フローF |
| P9 対象外の確認 | 第3部（外部勤怠・補完テンプレート・デフォルト値設定） |

## 8.3 フローと検証項目の対応

**「概要」で示した8本のフローが、それぞれ検証項目に落ちているかの一覧です。** 検証プランの `対応フロー` 列で追えます。

| フロー | 対応する検証項目 | 件数 |
|---|---|---|
| **フローA** 初期セットアップ | OP 39件 / CA 14件 | 53 |
| **フローB** 運用更新 | OP 11件 | 11 |
| **フローC** 誤りがあったときの修正 | OP 17件 / CA 25件 | 42 |
| **フローD** 管理者の登録・昇格 | OP 5件 / CA 3件 | 8 |
| **フローE** 再取り込み | OP 6件 | 6 |
| **フローF** 権限による違い | OP 3件 | 3 |
| **フローG** 中断・再開 | OP 2件 | 2 |
| **フローH** 誤りを直して取り込み直す | OP 2件 | 2 |

**フローHはフローCの後半にあたるため、C の一部と重なります。** フローBは手入力を含むため、OP-608〜617 が該当します。

## 8.4 検証に使うデータ

**CSVに入力するデータは `20260803_検証用データセット.tsv`（D1〜D5・25件）にまとめてあります。** 操作系プランの `操作手順・入力値` 列がこのIDを参照します。

環境の初期設定（Staging・アカウント・初期データ0件）は操作系の P0_前提 に、境界値（行数・サイズ・桁数・金額）は各検証項目の入力値に統合済みです。**同じ値を2箇所で管理しません。**

## 8.5 検証プランの読み方

**検証する人が記入するのは末尾2列だけです。** それ以外は読む列です。

| | 操作系（10列） | 計算系（9列） |
|---|---|---|
| **何を確認するか** | 段階／検証ID／対応フロー／検証内容 | 検証ID／対応フロー／検証内容 |
| **どう操作するか** | 操作手順・入力値／画面 | 入力値／画面 |
| **どうなれば合格か** | 期待値／確認ポイント | 期待値／根拠・確認ポイント |
| **記入する** | **実際の結果／判定** | **実際の値／判定** |

**2つに分けているのは、進め方が違うためです。** 操作系は段階の順に画面をたどる作業、計算系は値を変えて結果を突き合わせる作業です。混ぜると手順が読みにくくなるため統合していません。

---

# 9. 関連資料

| | |
|---|---|
| 開発者向けの仕様書 | `README.md`（§0〜§11） |
| 判断の記録 | `JUDGMENT_LOG.md`（29件） |
| 変更履歴と理由 | `CHANGELOG.md` |
| 引き継ぎノート | `HANDOFF.md` |
| エンジニアへの回答 | `20260728_エンジニア質問_回答と対応.md` |
| 画面モック | https://suguru789987.github.io/rakmy-timerecord-import-spec/ |
| 全体の入口（owner-mock） | https://github.com/suguru789987/rakmy-owner-registration-mock |

**タイムレコード全体の正本は owner-mock 側です。** 本リポジトリはインポート機能のみを扱います。
